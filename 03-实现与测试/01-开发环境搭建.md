# 开发环境搭建

> 版本：v0.3_20260822 | 03-实现与测试 | 详细版

## 1. 依赖清单

| 软件 | 版本 | 说明 |
|------|------|------|
| Node | >=22 | pnpm 9 |
| Python | 3.11 | uv |
| Docker Desktop | 最新 | pg/neo4j/redis/minio |
| Git | 最新 |  |

## 2. 目录

```
app/
 ├─ frontend/ (Vue)
 ├─ desktop/electron
 ├─ server/ (FastAPI)
 └─ cli/
docker-compose.yml
```

## 3. Docker Compose

```yaml
services:
  postgres:
    image: pgvector/pgvector:pg16
    environment: {POSTGRES_DB: app, POSTGRES_USER: postgres, POSTGRES_PASSWORD: postgres}
    ports: ["5432:5432"]
  neo4j:
    image: neo4j:5
    environment: {NEO4J_AUTH: neo4j/password}
    ports: ["7474:7474","7687:7687"]
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    ports: ["9000:9000","9001:9001"]
```

## 4. 后端启动

```bash
cd server
uv sync  # 含 fastapi, langgraph, mcp, pgvector, neo4j, redis, apscheduler, pydantic, sqlmodel
uv run alembic upgrade head
uv run uvicorn main:app --reload --port 8000
# 健康检查
curl http://localhost:8000/health
curl http://localhost:8000/api/v1/plans/stream?trace_id=test -N
```

## 5. 前端/桌面启动

```bash
pnpm install
pnpm dev              # Web 5173
pnpm electron:dev     # 桌面，需先electron-rebuild（better-sqlite3）
pnpm electron:build --dir  # 免安装调试
```

## 6. 环境变量 .env

```
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app
NEO4J_URL=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password
REDIS_URL=redis://localhost:6379
LLM_API_KEY=sk-...
LLM_BASE_URL=https://api.deepseek.com
```

## 7. 常见问题

- better-sqlite3编译失败 -> `pnpm rebuild` 或 `npm_config_arch`，失败则降级electron-store
- Neo4j内存 -> Docker限 `mem_limit: 2g`
- 端口占用 -> 改`docker-compose ports`

## 变更日志

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-08-22 | v0.3 | 补全Compose+启动+排错 |
