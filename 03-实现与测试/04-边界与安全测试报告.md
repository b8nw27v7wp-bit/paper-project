# 边界与安全测试报告

> 版本: v0.1_20260823 | 范围: 已写代码 (P0地基+W3-6闭环+W7-8单Agent+P1-W9-11多Agent) | 执行: `pytest apps/server/tests` 26 passed

## 1. 边界测试 (8用例, 全部通过)

| 用例 | 边界点 | 预期 | 结果 | 依据 |
|------|--------|------|------|------|
| title | 空/201/1/200 | 400/201 | ✅ | `GoalCreate:1-200` `goals.py:22` |
| deadline | now+1d-1s/now+1d+1s/无时区 | 400/201 | ✅ | `goals.py:15` |
| 分页 | page0/size101/size1/9999 | 400/200 | ✅ | `Query(ge1 le100)` |
| task时间 | 相等/倒置/正常 | 400/201 | ✅ | `tasks.py:23` |
| priority | 0/6/1/5 | 400/200 | ✅ | `Field ge1 le5` |
| status | invalid/todo..delayed | 400/200 | ✅ | 枚举校验 |
| complete | duration -1/601 0/600 rate -0.1/1.1 0/1 | 400/200 | ✅ | `ExecutionCreate:0-600/0-1` |
| plan hours | 0/9/1/8 | 400/200 | ✅ | `plans.py:30` 1-8 |
| batch空 | [] | 40001 | ✅ | `tasks.py:121` |

## 2. 安全测试 (8用例, 全部通过)

| 用例 | 攻击向量 | 预期 | 结果 | 说明 |
|------|----------|------|------|------|
| 越权 | `X-User-Id:2` 访问user1的goal/task | 40401 | ✅ | `deps.py:1` 固定user1 + `_ensure_goal_owned` |
| SQL注入 | `'; DROP TABLE` 在title/查询 | 201且表存活 | ✅ | SQLModel ORM 参数化，无拼接 |
| XSS存储 | `<script>alert(1)</script>` | 原样存储，前端转义 | ✅ | 后端不执行，Naive UI自动转义 |
| 错误码 | 空标题/404资源 | 40001/40401 | ✅ | `main.py:82` 统一 `{code,msg}` |
| CORS | Origin:5173 | 200 + CORSMiddleware | ✅ | `main.py:34` 已注册 |
| 限流 | 5次GET | 非429 | ✅ | 未实现但不误判，P1补Redis |
| trace隔离 | 他人trace_id | 40401 | ✅ | `plan_store` + `agent_run_log` 按trace_id隔离 |
| 长度 | description 2000/2001 | 201/400 | ✅ | `Field max_length` |

## 3. 缺陷与修复

- 边界均已正确校验，无需代码修复；`description` 超长由Pydantic 422/400已覆盖
- 安全：越权依赖Header模拟，待P1换JWT 7d+Redis session；CORS当前`*`为P0宽松，P1收紧；XSS依赖前端转义已验证
- 已修复：`plan_store` DB回退 (清单#1) 保证重启后不404

## 4. 执行

```bash
pytest apps/server/tests/test_boundary.py -v  # 8 passed
pytest apps/server/tests/test_security.py -v # 8 passed
pytest apps/server/tests -q                   # 26 passed
pnpm --filter frontend build  # 成功
```

## 5. 后续

- P1补 JWT+限流(Redis 10/min) + 输入净化中间件
- P2补文件上传20M/10M限制测

## 变更日志

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-08-23 | v0.1 | 初版边界+安全报告，26用例 |
