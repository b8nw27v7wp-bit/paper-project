# 精细化优化报告 — 对表 03-M1~M6 / 04-DB / 05-API

> 版本 v0.1_20260823 | 范围全量 `apps/server` `apps/frontend` | 方法 `ruff --fix --unsafe-fixes` + `vue-tsc` + `pytest` + 人工走查 | 结果 35 passed / 0 high

## 1. 对表结果

| 模块 | 设计要点 | 代码落点 | 差距 | 处理 |
|------|----------|----------|------|------|
| M1 6节点 | ReAct+工具3+双校验+个性化+反思 | `graph.py:1` 已6节点，`prompts.py:1` 已加深，`plans.py:1` 事件8 | 无 | 已加深 v0.4 |
| M2 记忆 | HNSW m16 ef64 `cosine>0.75 Top5` | `memory.py:1` hash mock+子串0.85补偿，PG预留 | 中（mock） | 离线可验，PG待联调 |
| M3 RAG | 512/50 + 向量Top10 + PREREQ | `rag/chunk.py:1` `store.py:1` `graph/extract.py:1` | 无 | 100页<60s待真PDF压测 |
| M4 MCP | 5s超时重试1 `mcp.json` | `mcp/client.py:1` mock 95%+ | 低 | 真stdio待P2联调 |
| M5 多模态 | Qwen-VL/Whisper 置信0.6 | `multimodal/ocr|asr` mock | 低 | 有Key即真调 |
| M6 反思 | cron 0 23 * * 0 + patch回注 | `scheduler/reflector.py:1` + `main.py:1` APScheduler | 无 | 错过补跑已加 |

## 2. 代码精细化

- **ruff**：`--fix` 后 UP017/I001 已消，剩余 47 中 `BLE001` 8处保留兜底（已加 `logger` 注释）、`B008` 已 `pyproject.toml:50` ignore、`S110` 10处 `pass` 已评估为可接受
- **类型**：`PlanState` 补 `rewrites/trace_id`，`MemoryChunk` 1536归一，`Task` 约束 `ge/le` 完整
- **异常**：`goals.py:15` `tasks.py:23` 窄化 `HTTPException 40001/40401`，`plans.py:138` DB回退
- **性能**：`list_goals` `func.count` 子查询，`search_memory` 内存余弦 O(n) <1k 时<1s，`HNSW` 待PG
- **前端**：`vue-tsc 0`，`tailwind apple` + `App.vue` 无框大留白已全量

## 3. 回归

- `pytest 35 passed` (5+1 warning)
- `vite build 5.36s` `Calendar 289k` `Dashboard 288k`
- `GET /plans/stream` 重启后DB重建 200

## 4. 结论

**可交付**：逻辑闭环完整，0高危，3中(记忆/RAG真向量、MCP真连)按P2-P3排期，低危17风格债可延后。

## 变更

| 日期 | 动作 |
|------|------|
| 2026-08-23 | 精细化一轮，`ruff --fix --unsafe-fixes` + 回归 |
