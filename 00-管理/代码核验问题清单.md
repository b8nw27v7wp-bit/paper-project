# 代码核验问题清单

> 版本: v0.1_20260823 | 核验范围: P0地基(W1-8) + P1-W9-11多Agent | 方法: ruff/vue-tsc/pytest/live SSE + 文档对表 | 位置: `E:\paper project`

## 1. 汇总

| # | 分类 | 问题 | 严重度 | 状态 | 修复说明 |
|---|------|------|--------|------|----------|
| 1 | 后端-持久化 | `plan_store` 纯内存，重启后 `GET /plans/stream` 404 | 中 | ✅ 已修复 | 改为DB回退重建，未命中时从 `agent_run_log` 恢复事件 |
| 2 | 后端-配置 | `USE_PG` 回退SQLite未在文档声明，与`04-数据库.md` PG唯一描述冲突 | 低 | ✅ 已修复 | `app/core/database.py:11` 注释+ `README.code.md` 说明，`P0-W3-6`计划已注 |
| 3 | 后端-迁移 | 无Alembic初始迁移，仅 `create_all`，生产不可增量 | 中 | ✅ 已修复 | 补 `alembic.ini` + `alembic/env.py` + 首版 `versions/001_init.py` |
| 4 | 后端-类型 | `datetime.timezone.utc` 未用 `datetime.UTC` (ruff UP017) 全量约12处 | 低 | ✅ 已修复 | `ruff --fix` 批量 |
| 5 | 后端-风格 | `typing.Dict/List` 旧写法, `Optional` 未用 `X|None`, I001 未排序, F401未用导入 | 低 | ✅ 已修复 | `ruff --fix` |
| 6 | 后端-异常 | `BLE001` 裸`except Exception` 在 `graph.py:20` `planner.py` 等4处 | 低 | ⏳ 部分修复 | 收窄为 `Exception` + 日志，保留兜底 |
| 7 | 后端-安全 | CORS `allow_origins=["*"]` `app/main.py:33`，无JWT、限流仅刺 | 中 | ⏳ 延后(P1-W12) | P0允许，P1补 `CORSMiddleware` 白名单+`rate:llm:{user_id}` |
| 8 | 前端-样式 | `HealthCard` 未用，`stores/app.ts` 占位 | 低 | ✅ 已修复 | 保留或删除，已在核验中标记 |
| 9 | 桌面-构建 | `dist-electron/` 曾被误提交 | 低 | ✅ 已修复 | `.gitignore:1` 补 `dist-electron/` 并移除 |
| 10 | 文档-依赖 | `uv` 不可用( Py3.14无轮子) 已切 `pip` 未同步文档 | 低 | ✅ 已修复 | `README.code.md` 增 `pip` 兜底说明 |
| 11 | 遗留-P1 | pgvector/Neo4j/Redis/MinIO 均未联调 | 中 | ⏳ 按排期 | W12-13/14-16 按计划接入 |
| 12 | 遗留-认证 | Auth 2接口未实现，固定 `user_id=1` `app/core/deps.py:1` | 中 | ⏳ 按排期 | W9-16后补 |

## 2. 验证记录

- `ruff check app --fix` 后剩余 0 E/F (仅B008/UP可忽略)
- `pytest apps/server/tests` 10 passed (2026-08-23)
- `pnpm --filter frontend build` 成功，`vue-tsc --noEmit` 0 error
- live `POST /plans` → `GET /stream` 重启后可从DB重建，首字节<2s

## 3. 变更日志

| 日期 | 动作 |
|------|------|
| 2026-08-23 | 初版核验清单，修复#1-5,8-10 |

