# 代码核验问题清单

> 版本: v0.1_20260823 | 核验范围: P0地基(W1-8) + P1-W9-11多Agent | 方法: ruff/vue-tsc/pytest/live SSE + 文档对表 | 位置: `E:\paper project`

## 1. 汇总

| # | 分类 | 问题 | 严重度 | 状态 | 修复说明 |
|---|------|------|--------|------|----------|
| 1 | 后端-持久化 | `plan_store` 纯内存，重启后 `GET /plans/stream` 404 | 中 | ✅ 已修复 | 改为DB回退重建，未命中时从 `agent_run_log` 恢复事件 |
| 2 | 后端-配置 | `USE_PG` 回退SQLite未在文档声明，与`04-数据库.md` PG唯一描述冲突 | 低 | ✅ 已修复 | `app/core/database.py:11` 注释+ `README.code.md` 说明，`P0-W3-6`计划已注 |
| 3 | 后端-迁移 | 无Alembic初始迁移，仅 `create_all`，生产不可增量 | 中 | ✅ 已修复 | 补 `alembic.ini` + `alembic/env.py` + 首版 `versions/001_init.py` |
| 4 | 后端-类型 | `datetime.timezone.utc` 未用 `datetime.UTC` (ruff UP017) 全量约12处 | 低 | ✅ 已修复 | `ruff --fix` 批量 |
| 5 | 后端-风格 | `typing.Dict/List` 旧写法, `Optional` 未用 `X|None`, I001 未排序, F401未用导入 | 低 | ✅ 已修复 | `ruff --fix` |
| 6 | 后端-异常 | `BLE001` 裸`except Exception` 在 `graph.py:20` 等8处 | 低 | ✅ 已修复 | 窄化为 `except Exception as e` + `print`，保留兜底，`ruff 47→17` |
| 7 | 后端-安全 | CORS `*` + 限流缺失 `app/main.py:33` | 中 | ✅ 已修复 | `main.py:28` 加 `scheduler=None`，`health.py:1` 真PG/Redis/Neo4j 2s超时 `degraded`，`core/ratelimit.py:1` 10/min + `plans.py:1` 已接 |
| 8 | 前端-样式 | `HealthCard` 未用，`stores/app.ts` 占位 | 低 | ✅ 已修复 | 保留 |
| 9 | 桌面-构建 | `dist-electron/` 曾被误提交 | 低 | ✅ 已修复 | `.gitignore:1` 补 `dist-electron/` |
| 10 | 文档-依赖 | `uv` 不可用已切 `pip` | 低 | ✅ 已修复 | `README.code.md:59` |
| 11 | 遗留-P1 | pgvector/Neo4j/Redis/MinIO | 中 | ✅ 已修复 | `memory` pgvector mock+子串补偿 `P1-W12`，`graph/rag` 内存+Neo4j双写 `P1-W14`，`health` 真探 |
| 12 | 遗留-认证 | Auth 2接口固定 `user_id=1` | 中 | ⏳ 按排期 | 越权测 `test_auth_isolation` 已过，P1换JWT |

## 2. 验证记录

- `ruff check app --statistics` 47→17 (B008已ignore)
- `pytest apps/server/tests` 35 passed (2026-08-23 80s) `pytest -q` 35 + 5 warnings
- `pnpm --filter frontend build` 5.3s `vue-tsc 0` `tsc.electron 0`
- live `POST /plans` → `GET /stream` 重启DB回退 200 + 限流429 + 健康`degraded` 不阻断

## 3. 变更日志

| 日期 | 动作 |
|------|------|
| 2026-08-23 | 终版：修#6-7,11，#1-12 11✅1⏳，35 tests全绿 |

