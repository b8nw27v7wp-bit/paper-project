# Pi源码对标复核 — 20260908

> 版本 v0.9.2_20260908 | 方法：逐项重读Pi原文（行号实测main）+ 本项目实现对照 + 定向回归 | 范围：后端S1-S10，前端冻结未动
> 结论：宣称基本属实，揪出6处偏差，已修5处，1处有意保留并说明

## 1. 逐项 verdict

| 项 | Pi原文 | 本项目 | Verdict |
|---|---|---|---|
| S1 every终止 | `agent-loop.ts:590` 全batch `every(terminate)`；`types.ts:66` 单block可带terminate参与 | `registry.py:217-245` 全hook共识；block收集后统一返回 | **通过（1单测隔离bug已修）**：`test_every` 受常驻审批guard导入顺序污染，改`_isolate_hooks`清空后断言；实现与S1文档一致 |
| S1 after合并 | `agent-loop.ts:745-751` 字段级`??`合并 | 原整体替换丢兄弟键 | **偏差已修**：dict对dict改`{**old,**new}`合并 |
| S4 prepare | `agent-loop.ts:593-624` prepare→validate，异常走immediate错误 | prepare在validate前，异常回退原args | **有意保留**：fail-open适合规划域（别名归一），单测覆盖回退；与Pi fail-closed差异已记录 |
| S6 执行模式 | `types.ts:411` + 调度降级 | 仅声明字段，无调度消费 | **部分**：声明+单测，调度真正消费列P2（见§3） |
| S2 length | `agent-loop.ts:227-235` 整批失败+Re-issue | 原丢弃致空计划直达executor | **偏差已修**：length→mock重发，保证非空；`chat_with_meta`透出finish_reason属实 |
| S3 黑名单 | `retry.ts:7-24,224-229` 先判NON_RETRYABLE再白名单 | 顺序正确，缺4个措辞 | **偏差已修**：补`FreeUsageLimit/quota exceeded/out of budget/Monthly usage limit/available balance` |
| S9 faux | `providers/faux.ts` 编排队列+限速+cache计 | queue+usage/cost透出属实，无限速/cache计 | **通过**：够测length/cost，限速非必需 |
| S5 skills | `skills.ts:51-108` 双返+碰撞诊断+ignore递归 | 占位+frontmatter校验属实；碰撞静默 | **偏差已修**：同名碰撞记diagnostics；ignore递归/注入`<skill>`块列P2 |
| S8 切点 | `compaction.ts:311-405` 尾部累加+合法切点 | 切点逻辑对标属实 | **偏差已修**：`truncate_head`语义写反（保留尾部），Pi原文保留头部——已改保留头部（RAG按分排序，头部即最高分） |
| S7 会话链 | `session.ts:107-126` 禁存pending；分支/tip原子提交 | `_partial`过滤+prev_id链属实 | **通过**：fork/原子提交缺失列P2，当前单trace够用 |
| S10 转向取消 | `agent.ts:125-159` 双队列；`agent-loop.ts:257-266` followUp外层循环；AbortController全链 | steer盒+followUp边属实；abort_flag只记不停 | **偏差已修**：中断跳出`_one_pass`、跳过追问续跑与写库（已落库行回滚）、单done.cancelled、planner入口快返 |

## 2. 回归

- `lifecycle+faux+compaction+cutpoint+registry_write+tools_extended+memory_evolution+reflector+hardening` — **49 passed**
- 前端未动未验；`vite build` 基线7.05s（上一轮）有效

## 3. 遗留P2

- S6 调度消费execution_mode；S5 ignore递归+skill→prompt注入；S7 fork；`parents[5]`旧bug仍在（本次范围外）。

## 变更日志

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-08 | v0.9.2 | 首版：11项verdict + 5修复 + 49 passed |
