# Codex对标借鉴 — 20260908

> 版本 v0.9.3_20260908 | 基座：`E:\codex yuanma`（openai/codex shallow main，Rust实现，只搬机制）| 与Pi分工：Pi补纵深机制（hooks/重试/会话链），Codex补横向制度（审批policy/exec语义/配置工程）
> 约束：不改栈；SSE契约只增不减；旧布尔`require_approval`/旧`trace_id`接口全兼容

## 1. Wave A — 审批制度（`protocol.rs:984` + `execpolicy/`）

- `app/services/exec_policy.py`：`Decision{Allow,Prompt,Forbidden}` + `_PREFIX_RULES`前缀max匹配 + 启发回退（写Prompt/读Allow/删批量Forbidden）。
- `plans.py`：入参`approval: untrusted|on-request|never|granular`（默认on-request；旧`require_approval=true→untrusted`）；Forbidden直拒不进Redis；`approval_required.data.available_decisions`；`POST /plans/{trace_id}/approve-rule`规则追加幂等。
- 测试：`tests/test_approval_policy.py` 5用例。

## 2. Wave B — 执行与会话语义（`exec/cli.rs:36-69` + thread/rollout）

- `PlanCreate`加`resume/fork/ephemeral/output_schema`；resume支持trace直通/name标题/`--last`；fork新id记`forked_from_seq`；ephemeral仅内存（跳Task/log/Redis）；output_schema校验失败mock重算。
- `GET /plans/{trace_id}/events?after_seq=N`增量续播 + `GET /plans/{trace_id}/last`终态；`filter_rollout`剔researcher中间start噪音（live cache不过滤保SSE契约）。
- 测试：`tests/test_thread_semantics.py` 5用例。

## 3. Wave C — MCP/配置工程化（`mcp_cmd.rs` + `mcp_types.rs` + `agents_md.rs`）

- `mcp.json`：每server `timeout/call_timeout/enabled_tools/disabled_tools/approval` + stdio/http互斥注释。
- `client.py`：`validate_mcp_config()`互斥校验；server级超时透传；`resolve_bearer`只读env（明文warning忽略）；`list_servers_status()`健康聚合。
- `prompts_loader.load_layered_agents_md`：cwd向上分层收集+来源标注+6000字节预算。
- 测试：`tests/test_mcp_config.py` 4用例。

## 4. 回归

- 新单测14（policy5+thread5+mcp4）+ 关联9（lifecycle4+faux3+cutpoint2）— 23 passed；plans子集3 passed。

## 变更日志

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-08 | v0.9.3 | 首版：A/B/C三波落地映射 |
