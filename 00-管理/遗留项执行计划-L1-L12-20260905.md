# 遗留项执行计划 L1–L12 — 20260905

> 版本 v1.0_20260905 | 依据：`待优化与补全清单.md` §6（L1–L12）+ `计划-代码匹配度核验-20260904.md` §4/§5 | 基线：pytest 40 passed (82s) · 双份 `app/` 字节级一致（0 diff）· `apps/server/.env` 从未入库（git log --all 无记录）· mcp.json 3 server mock · 无 JobStore
> 执行方式：子 agent 分两波并行（按文件冲突域分批），每波结束跑定向测试；全量回归后统一回写文档。

## 0. 全局约束

- 栈锁定：Vue3+FastAPI+LangGraph+pgvector，不新增依赖（L7 eslint 依赖除外，需在本文档 §5 登记）
- **双份 app/ 同步**：根 `app/` 与 `apps/server/app/` 必须保持一致，每个 agent 修改后逐文件复制同步
- 每项完成即跑定向 pytest；全量回归：`py -m pytest tests/ -q`（≥40 passed）+ `npm run check`（turbo: vue-tsc+vite build）+ `ruff check`
- 事件契约不变：SSE 8 事件、`/plans?mode=multi` 6 节点、trace_id/log 查询语义保持兼容

## 1. 分批与冲突域

| 波次 | Agent | 条目 | 主要文件（互不重叠） |
|---|---|---|---|
| Wave1-A | A | L1 假流式→真事件流 | app/api/v1/plans.py、app/services/planner.py、app/agents/graph.py(仅事件发射)、tests/test_plans.py |
| Wave1-B | B | L4 Redis 限流验证 + L6 prod 鉴权强拒 | app/core/ratelimit.py、app/core/deps.py、tests/test_security.py |
| Wave1-C | C | L9 server 容器 + L12 jobstore 持久化 | docker-compose.yml、docker/Dockerfile.server(新)、app/main.py、app/scheduler/reflector.py、app/core/config.py |
| Wave1-D | D | L10 实验真实化 + L3 番茄钟接口 | scripts/experiment_blind.py、app/api/v1/tasks.py、tests/test_goals_tasks.py |
| Wave2-E | E | L5 ruff BLE001 窄化 + L7 eslint 复核 | app/agents/tools/registry.py、app/graph/*、apps/frontend/package.json、eslint 配置 |
| Wave2-F | F | L11 MCP 真 stdio server 接入 | mcp.json、app/mcp/client.py、app/mcp/servers/*(新)、tests/test_p2.py |

> Wave2 依赖 Wave1 完成（graph.py/plans.py/main.py 在 Wave1 有改动，避免并发冲突）。

## 2. 逐项改法与验收

### P0

- **L2 密钥核实**：git 历史从未跟踪 `apps/server/.env`（`git log --all -- "**/.env"` 无记录）。处置：确认 `.gitignore` 覆盖 `.env`；`LLM_API_KEY` 轮转为用户侧动作，文档标注"未入库无需轮转历史，建议换 key 以防本机泄露"。验收：`git ls-files | grep .env` 仅 `.env.example`。

### Wave1-A（L1，P1）

- **改法**：`POST /plans` multi 路径改用 `graph.astream`（或 `astream_events`）透传真实节点事件，删除 plans.py:587 `sleep(0.08)` 伪流；保持 8 事件契约（planner_started→…→done/critic_feedback/mentor_msg）；单 Agent 降级路径保留；Redis `cache:workbench` 5m 重放与 `agent_run_log` 全量落库不变。
- **验收**：`pytest tests/test_plans.py` 全过；SSE 事件顺序与 6 节点执行序一致；无 `sleep(0.08)`；events 全量落库不回退。

### Wave1-B（L4+L6，P1）

- **L4**：代码已 Redis 优先（ratelimit.py:18 `_get_redis`，key `user_id:ip:path` 归一）。补验证：Redis 不可达回退内存、INCR+EXPIRE 计数、429 行为；补测试用例（monkeypatch Redis 不可达）。
- **L6**：deps.py prod（debug=False）且非 pytest 时，`X-User-Id` 无有效 JWT → 强制 401（替换现 51 行警告）。dev/debug 回退保留。
- **验收**：`pytest tests/test_security.py` 全过；新增 prod 强拒用例。

### Wave1-C（L9+L12，P1/P2）

- **L9**：docker-compose 增 `server` 服务（`docker/Dockerfile.server`：python slim + uvicorn app.main:app，healthcheck /health，depends_on pg/redis health）；nginx 代理 server:8000 落到实源；不破坏现有 5 服务。
- **L12**：`main.py:43` AsyncIOScheduler 增可选 SQLAlchemyJobStore（DATABASE_URL 为 PG 时启用，表 `apscheduler_jobs`；SQLite/无 PG 时回退 MemoryJobStore），config.py 加开关；reflector.py 注册逻辑适配。
- **验收**：`python -c "from app.main import app"` ok；pytest 全量不回归；`docker compose config` 校验通过。

### Wave1-D（L10+L3，P1/P2）

- **L10**：experiment_blind.py 增 `--source db|simulate`：db 模式从 `agent_run_log`/`execution_log` 真实回放（无数据时明确报错并提示），simulate 保留且输出标注"模拟数据"；论文口径可切换。
- **L3**：tasks.py 增 `POST /tasks/{id}/pomodoro`（duration 1–600s、focus 0–1，写 execution_log），复用 complete 权限与校验风格；补测试。
- **验收**：定向 pytest 全过；db 模式空库时行为明确。

### Wave2-E（L5+L7，P2）

- **L5**：registry.py/graph 等剩余约 8 处 BLE001 裸 except 窄化（具体异常类型 + logger.warning），ruff 目标 0 E 或仅剩已豁免项。
- **L7**：补装 eslint devDependencies（eslint+eslint-plugin-vue+typescript-eslint），跑通 `lint:eslint`；记录新增依赖于 §5。
- **验收**：`ruff check` 输出记录；`pnpm --filter frontend lint:eslint` 可运行；`npm run check` 全过。

### Wave2-F（L11，P2）

- **改法**：新增 `app/mcp/servers/`（基于 mcp SDK stdio 的本地真 server：calendar→SQLite、todo→SQLite、search→本地确定性工具）；mcp.json 三 server 从 `command:"mock"` 切到 `python -m app.mcp.servers.*`；client.py 真 stdio 路径（3s 超时×3 重试）为主、失败回退 mock 保持演示零依赖。
- **验收**：`pytest tests/test_p2.py` 全过；`POST /mcp/call` 走真 stdio 成功（有进程级证据）。

## 3. 执行记录（2026-09-05 填实）

| 条目 | 状态 | 执行者 | 证据(文件:行号/测试) | 回归 |
|---|---|---|---|---|
| L1 | ✅ | Agent-A | plans.py:349 `_astream_run`（graph.astream stream_mode="updates" 真节点事件流）、:322 回退拼装保留、伪流 sleep(0.08) 已删；test_plans.py 8 passed（+2：事件序/无伪流） | 全量通过 |
| L2 | ✅ | 主控 | git log --all 无 .env 入库记录；.gitignore:14 覆盖；仅 .env.example 被跟踪 | — |
| L3 | ✅ | Agent-D | tasks.py `POST /tasks/{id}/pomodoro`（1-600s/0-1，写 execution_log，delay_reason="pomodoro"）；test_goals_tasks.py 5 passed（+2） | 全量通过 |
| L4 | ✅ | Agent-B | ratelimit.py INCR+EXPIRE 确认真生效（cnt==1 设 60s TTL）；修冷却重试(:18-40)与异常回退(:63-83)2缺陷；key user_id:ip:path；test_security.py 15 passed（+7） | 全量通过 |
| L5 | ✅ | Agent-E | agents/graph/mcp 三目录 `ruff --select BLE001,S110,RUF100` = All checks passed（前 119 处归零）；窄化具体异常或 logger.warning(exc_info=True)，行为不变 | 全量通过 |
| L6 | ✅ | Agent-B | deps.py:42-55 prod 无有效 JWT → 401 (code 40101)；debug/pytest 回退保留；前端 axios Bearer 优先已确认；test_security.py prod 强拒 3 用例 | 全量通过 |
| L7 | ✅ | Agent-E | eslint 10.10+eslint-plugin-vue 10.10+typescript-eslint 8.69+@eslint/js 已装（pnpm +73包）；lint:eslint 0 errors/5 warnings；eslint.config.js 调 essential 宽松子集 | npm run check 通过 |
| L9 | ✅ | Agent-C | docker/Dockerfile.server(新)+.dockerignore；compose 增 server 服务(6服务，healthcheck/depends_on)；nginx.conf 代理改 server:8000；yaml 解析校验通过（本机无 docker CLI） | import ok / pytest 过 |
| L10 | ✅ | Agent-D | experiment_blind.py `--source db\|simulate`；db 模式 agent_run_log(critic llm维度) + execution_log(memory时间维度) 真实回放，空库 exit 1；simulate 标注"不可引用" | 两模式实测 |
| L11 | ✅ | Agent-F | app/mcp/servers/{calendar,todo,search}.py（SQLite WAL/本地确定性）；mcp.json 切 `py -m app.mcp.servers.*`；回退链 真调用→is_error→spawn失败→mock；test_p2.py 8 passed（+3） | 全量通过 |
| L12 | ✅ | Agent-C | config.py `scheduler_jobstore_url` 开关；main.py PG 时 SQLAlchemyJobStore（postgresql+psycopg 归一化）+ replace_existing 幂等，SQLite/测试回退内存 | import ok / pytest 过 |

> 回归汇总（2026-09-05 主控复跑）：pytest **54 passed** (114s) · `npm run check` 1 successful (vue-tsc+vite build 6.13s) · 双份 `app/` 71 文件 **0 diff**。
> 新发现风险（移交后续）：① prod 下 SSE EventSource 无法携带鉴权头，将被 L6 强拒 401，建议改 fetch 流式或一次性 ticket；② L10 db 模式实验2 分组（memory 时间维度）数据积累后应换 per-task 标记。

## 4. 文档回写清单（完成后）

- `待优化与补全清单.md` §6 状态列 + §7 变更记录
- `计划-代码匹配度核验-20260904.md` §4/§5 处置列追加 20260905 行
- `P2-W17-22-执行计划.md`、`P3-W23-26-执行计划.md` 落地状态表同步
- 本文档 §3 执行记录填实

## 5. 新增依赖登记（已装）

| 依赖 | 位置 | 用途 | 来源条目 |
|---|---|---|---|
| eslint ^10.10.0 | apps/frontend devDependencies | lint:eslint 可运行 | L7 |
| eslint-plugin-vue ^10.10.0 | apps/frontend devDependencies | Vue SFC 规则 | L7 |
| typescript-eslint ^8.69.0 | apps/frontend devDependencies | TS 解析 | L7 |
| @eslint/js ^10.0.1 | apps/frontend devDependencies | eslint.config.js 推荐集配套 | L7 |
| typescript 5.7→5.9.3 | apps/frontend devDependencies | add 时被动刷新（原已有） | L7 |

> 后端零新增依赖（apscheduler/sqlalchemy/mcp SDK 均为原有）。

## 6. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-05 | v1.0 | 首版：L1–L12 分两波执行计划 + 全局约束 + 回写清单 |
| 2026-09-05 | v1.1 | 执行完成：Wave1(A/B/C/D)+Wave2(E/F) 六 agent 并行落地 L1/L3-L7/L9-L12，L2 核实闭环；§3 执行记录填实；回归 pytest 54 passed + npm run check 通过 + 双份app/ 0diff；新遗留：prod SSE 鉴权头风险 |
