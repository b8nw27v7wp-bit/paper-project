# Agent能力基线审查 — 20260908

> 版本 v0.8_20260908 | 基线 P0-P3 94% | 范围：SystemAgent单点 + 6节点图 + 注册表4工具 + 统一LLM + plan_store/checkpoint/compaction + API/SSE/工作台 + MCP/skills | 方法：只读审查（graph.py/system_agent.py/registry.py/llm.py/plans.py/agent.py/client.py/workbench）

## 1. 状态表

| 模块 | 证据 | 状态 |
|---|---|---|
| SystemAgent单点 | `apps/server/app/agents/system_agent.py:16 name=studying-planner, version 0.3.0, get_manifest/ainvoke/plan_project` | 正常 |
| 6节点图 | `apps/server/app/agents/graph.py:746 build_graph planner/researcher/executor/critic/mentor/reflector` | 正常（本次拓展后7节点，见05） |
| Planner/Researcher/Executor | `graph.py:98 llm_generate降级mock+patch重分配, :201 execute_tool三检索并行, :297 write_tasks落库+降级透传` | 正常 |
| Critic/Mentor/Reflector | `graph.py:370 规则三检+_try_llm_critic15s降级打标, :450 四维话术, :500 patch{reduce_load/add_buffer/reorder/reallocate}` | 正常 |
| 注册表 | `app/agents/tools/registry.py:114 register/schema/execute_tool, 321 memory_search/rag_search/graph_search/write_tasks` | 正常，单一职责可热插 |
| 统一LLM | `app/core/llm.py:92 PROVIDER_MAP+FALLBACK_ORDER+独立env key+embed回退` | 正常，provider无关 |
| plan_store/checkpoint/compaction | `app/services/planner.py:407 PlanStore内存+DB, app/core/checkpoint.py:21 FileMemorySaver WAL, app/agents/compaction.py:28 20条/8000字符双触发` | 正常 |
| API | `app/api/v1/plans.py:750 POST /plans?mode=multi astream真节点流+6条agent_run_log, app/api/v1/agent.py:13 manifest/health/tools/plan` | 正常 |
| MCP/skills | `app/mcp/client.py:467 3s超时3次退避, mcp.json calendar/todo/search真stdio, skills/graph/memory/planner/rag` | 正常 |
| 调度自演进 | `app/scheduler/reflector.py:16 _week_str/replan_stats/normalize_patch, app/main.py:42 jobstore开关` | 正常 |
| 前端工作台 | `apps/frontend/src/views/AgentWorkbenchView.vue(1477行) + stores/workbench.ts:56 SSE+ticket+Graph/Inspector` | 正常 |

## 2. 风险清单

| # | 风险 | 位置 | 说明 |
|---|---|---|---|
| R1 | `__init__.py` stub | `app/agents/__init__.py:1` | 无导出，便利性差，不影响运行 |
| R2 | prompts双源 | `app/agents/prompts/`空目录 vs 根`prompts/` | `prompts_loader`硬编码绝对路径，可移植差 |
| R3 | `parents[5]`越界吞错 | `registry.py:273` | 实际4级parents，靠try回退`Path("mcp.json")`，脆弱但可用 |
| R4 | 双份app漂移 | 根`app/`(gitignore) vs `apps/server/app/` | cwd指错致SQLite落库漂移风险，见mcp.json注释 |
| R5 | Executor双写路径不一致 | `plans.py:1093`直插 vs registry校验 | 审批回退绕过goal归属/50条校验（本次P2保留，后续统一） |
| R6 | 单文件过大 | 工作台1477行+workbench556行 | e2e仅smoke1，回归薄 |

## 变更日志

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-08 | v0.8 | 首版：6节点基线审查，为能力拓展（05）前置 |
