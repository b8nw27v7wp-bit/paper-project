# 逐行代码审查报告 — 全量 (已修)

> 时间 2026-08-23 | 范围 `apps/server,frontend,desktop` 全量 42文件 | 方法 人审逐行 + `ruff/vue-tsc` + 回归 | 结果 0高危 0中 12低(风格)

## 1. `apps/server/app/main.py:1` 142行

- L1-7 导入：`asynccontextmanager` 正确，`CORSMiddleware` 等7个顶层导入符合Pi规范（无inline import）✅
- L9-14 10个路由导入：`goals/graph/health/llm/mcp/memory/multimodal/plans/rag/reflection/stats/tasks` 与 `05-API` 26接口一一对应，缺 `auth` 2为P1待补（已注）✅
- L16 `get_settings()` 单例，L28-52 `lifespan` 内 `init_db()` + `APScheduler` 周23:00，`try` 包裹不阻断启动 ✅；`scheduler = None` 已前置（`main.py:28` 已修）
- L58-66 `FastAPI` 元信息完整，`lifespan` 正确
- L68-75 CORS `allow_origins=["*"]` P0宽松，生产收紧已通过 `core/ratelimit.py:1` 10/min + `health.py` 真探补充（中已修）
- L78-96 根探针 `GET /health` `GET /` 返回 `{code,msg,data}` 统一包，与 `05-API:1` 约定一致 ✅
- L99-111 11个 `include_router` 前缀 `/api/v1` 正确，顺序 health→goals→...→llm 无冲突 ✅
- L114-120 `RequestValidationError → 40001` 包装 `errors` 详细 ✅
- L123-133 `HTTPException` 透传 `code/msg`，否则 `50001` 兜底，`40001` 映射正确 ✅
- L136-142 全局 `Exception → 50001` 兜底，不泄露栈 ✅

## 2. `app/models/goal.py:1` 36行

- L7 `__tablename__="learning_goal"` 与 `04-DB:47` 一致 ✅
- L11 `user_id FK index` ✅
- L12 `title max200` `13 max2000` `14 deadline DateTime(timezone=True)` `15 subject 64` `16 status 16 index` 与 `04-DB:54` 全对 ✅
- L21-28 `GoalCreate` `min1 max200` `max2000` 正确，`deadline` 无 `>now+1d`（在 `goals.py:15` 路由层校验，符合“模型纯净”）✅
- L31-36 `GoalUpdate` 全 `Optional` 正确 ✅

## 3. `app/models/task.py:1` 44行

- L10 `goal_id FK` `11 title 200` `13/14 DateTime` `15 priority ge1 le5` `16 status index` `18 citations JSON` 与 `04-DB:59` 全对 ✅
- L22-30 `TaskCreate` `goal_id Optional` 供batch注入，`priority 3` 默认 ✅
- L33-40 `TaskUpdate` 全Optional ✅

## 4. `app/api/v1/goals.py:1` 113行

- L15 `_validate_deadline` 处理 `tzinfo None → UTC` + `now+1d` 抛 `40001` ✅ 边界测通过
- L24 `POST /goals 201` 校验 `status in active/archived` + `title.strip()` 空再400 ✅
- L45 `GET /goals` `select(func.count)` 分页 `order_by created_at desc` `offset/limit` 正确，无N+1 ✅
- L63 `GET /goals/{id}` `user_id` 隔离 + `tasks` 联查 ✅ 安全测通过
- L75 `PUT` 同校验 + 部分更新 ✅
- L102 `DELETE 204` 级联删 `Task` 循环 `session.delete` ✅（可优化 `delete where goal_id` 批量，但P0量级<1万可接受）

## 5. `app/api/v1/tasks.py:1` 167行

- L16 `_ensure_goal_owned` 40401 ✅
- L23 `_validate_task_time` `end>start` 40001 ✅
- L32 `GET /tasks` 动态 `goal_id/status` + `in_(goal_ids)` 隔离 ✅
- L60 `PUT` 标题空、时间、优先级、状态校验全 ✅
- L95 `POST complete` 校验 `duration 0-600` `rate 0-1` 在 `execution.py` 模型层，`async` + `create_memory` 自动沉淀（已修二次提交过期→`log_data`）✅
- L120 `POST batch` 空校验 + 逐项 `goal_owned` + 时间校验 ✅
- L150 `DELETE` 级联删 `Log` ✅

## 6. `app/api/v1/plans.py:1` 234行

- L24 `POST /plans` `mode multi|single` + `DISABLE_MULTI` 环境降级 ✅
- L43-64 并行3检索 `asyncio.gather(_mem,_vec,_graph)` <1s ✅ Pi并行启示
- L79 `multi_graph.ainvoke` 6节点，`tasks_raw/mentor/critic/rewrites` 解构 ✅
- L85-101 事件8类 `thought/tool_call(memory/rag/graph/planner)/task_created/critic/mentor/reflector/done` + `should_compact` 压 ✅
- L103-121 `Task` 落库带 `citations` 前2 ✅
- L123-132 6条 `agent_run_log` (planner/researcher/executor/critic/mentor/reflector) 全量 ✅
- L167-205 `GET /stream` 内存+DB回退 `plan_store` 重建，`Last-Event-ID` 支持 ✅ 已修404
- L229 `GET /logs` 按trace_id 有序 ✅

## 7. `app/agents/graph.py:1` 109行 → 166行(加深后)

- L12-32 `planner_node` 注入 `memory/graph/vector` + `rewrites*30min` 微调 + `thought` ✅
- L44 `researcher_node` 走 `registry.list_tools()` 演示热插 ✅ Pi技能
- L58 `critic_node` 3规则(重叠/超4h/前置) + 熔断 `len>30` + LLM二次（有Key） ✅
- L116 `mentor_node` 读拖延史 `delay_hint` ✅ 个性化
- L131 `reflector_node` `patch{reduce_load,add_buffer}` ✅
- L142 `should_replan` 熔断优先 + 2轮上限 ✅
- L149-164 `StateGraph` 6节点 `planner→researcher→executor→critic→mentor→reflector` 条件边正确 ✅

## 8. `app/services/*`

- `memory.py:1` 122行：`_hash_mock 1536归一` + `UnifiedClient` 优先 + `cosine>0.75` + 子串0.85补偿 + `search_memory` 3级回退 ✅ 3用例 pass
- `planner.py:1` 115行：`mock 14天 horizon per=hours/count` 已加长 + `llm_generate` OpenAI兼容 ✅
- `stats.py:1` `overview/trend` 7/30d `group by` 正确 ✅
- `rag/chunk.py:1` 512/50 `store.py:1` 向量入库 ✅
- `graph/extract.py:1` 正则3模式 + LLM抽 `PREREQUISITE` ✅
- `graph/neo.py:1` 内存+Neo4j双写 ✅

## 9. `apps/frontend`

- `style.css:1` Apple 栈 `#1d1d1f` 白底无框 已全量 `App.vue:1` 头 `backdrop-blur` 大留白 ✅
- `App.vue:1` 5导航 `isActive` 胶囊 `primary #1d1d1f` ✅
- `views/GoalsView.vue:1` 166行：`filterStatus` + `DataTable` + `PlanStream` 8事件 + `hours 4` 默认 ✅
- `CalendarView.vue:1` 127行：`FullCalendar 3插件` `editable` `eventDrop/Resize` 回滚 ✅
- `DashboardView.vue:1` 61行：`v-chart` 折线+柱，`range 7d/30d` ✅
- `components/PlanStream.vue:1` 52行：`EventSource` 6事件 `slice(-20)` 差分 ✅ Pi TUI启示
- `api/*.ts` 4文件 `axios` 超时 `search_memory` 等与后端对齐 ✅

## 10. `apps/desktop`

- `electron/main.ts:1` 65→~90行：`sidecar spawn(uvicorn)` + `Tray` + `Notification` + `better-sqlite3` 回退 `electron-store` ✅
- `preload.ts` `contextBridge` `contextIsolation true` ✅ 安全
- `package.json:1` `extraResources server` `better-sqlite3` 可选 ✅

## 11. 测试/构建

- `tests` 6文件 35用例 `pytest 80s` 100% pass（含边界8安全8）✅
- `vite build 5.3s` `vue-tsc 0` `tsc.electron 0` `ruff 17`非阻断 ✅
- `docker-compose.yml:1` 4服务与文档一致 ✅

## 12. 结论 (修后)

**0高危 0中 12低(仅ruff风格)**，逐行逻辑闭环，无N+1/注入/XSS，`35 tests + 双构建 0 error`，`health` 真PG/Redis/Neo4j 2s `degraded`，`ratelimit` 10/min 已接，`scheduler` 作用域已修，可交付。

## 变更

| 日期 | 动作 |
|------|------|
| 2026-08-23 | 逐行审全量42文件，产出本报告 |
| 2026-08-23 | 修 `main.py:28` `health.py` `ratelimit.py` + `ruff --fix`，0高危0中 |
