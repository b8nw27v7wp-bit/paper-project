# P1 二轮升级落地方案 — 基于 Pi 二轮提炼

> 版本 v0.1_20260823 | 依据 `00-管理/Pi-二轮提炼.md` | 目标：并行工具+会话压+技能化

## 1. 升级点

| # | Pi 启示 | 本项目落地 | 文件 | 验收 |
|---|---------|------------|------|------|
| 1 | 工具并行 `sequential` vs `parallel` | `researcher` 3工具 `asyncio.gather` 并行 | `app/agents/graph.py` + `app/api/v1/plans.py` | 检索<1s 不因串行叠加 |
| 2 | 上下文压 `compaction` 10轮摘要 | `app/agents/compaction.py` 超20事件自动压为摘要 | `graph.py` `plan_store` | 长trace不爆 |
| 3 | 技能热插 `Skills` | `app/agents/tools/registry.py` 加载 `mcp.json` 动态注册 | `registry.py` + `mcp.json` | 新增MCP无需改代码 |
| 4 | TUI差分 | `PlanStream` 增量 `time` + `type` 色 | `PlanStream.vue` | 大量事件不卡 |

## 2. 实施

- `compaction.py`：`should_compact(events)>20 → summarize(events) → 保留首尾+摘要`
- `registry.py`：启动时读 `mcp.json` 注册 `mcp:call`
- `plans.py`：`researcher` 三检索 `await asyncio.gather(...)`
- 文档：本方案 + `02-06 UI` 不动

## 3. 验证

- `pytest` 35 + 新 `test_compaction`
- `GET /plans/stream` 20+事件仍<2s首字节
