# 智能体短板修复与设计完善 — 20260905

> 版本 v1.0_20260905 | 依据：20260905 双 agent 架构勘察报告（5 短板）| 前置：L1-L12 已闭环（ad8889f）
> 执行方式：两波子 agent 并行（Wave1 A/B/C/E 文件域互不重叠 → Wave2 D 归拢 graph.py）

## 0. 全局约束

- 栈锁定不新增依赖（MCP search 联网用已有 httpx；token 压缩用字符预算近似）
- 双份 app/ 同步（字节级）；SSE 8 事件契约与 `/plans?mode=multi` 6 节点语义不破坏（响应 JSON 只增不删）
- 每项完成即跑定向 pytest；全量回归 pytest ≥54 + `npm run check`

## 1. Wave1（并行）

| 项 | 短板 | 改法 | 证据 | 测试 |
|---|---|---|---|---|
| A | `write_tasks` 占位未落库 | registry.py:366-467 真落库：schema 校验补 array/object、goal 越权 40401、时间倒置 40001、批内+库内幂等去重、单次 commit；fn 自开 Session 或复用 kw 注入（与 memory_search 同构） | registry.py:366-467、tests/test_registry_write.py（7 用例） | 15 passed |
| B | 记忆/RAG 同步检索查询向量 hash mock 失配 + O(n) 绕过 pgvector | memory.py 新增 `embed_flagged`（同源+mock 标注）与 `pg_vector_search`（SQL `<=>` cosine_distance）；asearch 双路径真同源；store.py 删"循环内静默假向量"改显式 RuntimeError；/memory/search 与 /rag/search 端点转 async 走异步链；结果逐条带 `emb=real\|mock` | memory.py:33-167、store.py:16-147、api/v1/memory.py:28-35、api/v1/rag.py:73-88 | 20+18 passed |
| C | compaction 仅事件计数级 | 双触发（>20 条 或 >8000 字符）：预算超限从头2尾2向中间收缩至预算内，剩余压 1 条 `compact_summary`（含 dropped+type_counts）；纯函数幂等；done 恒在 | compaction.py:9-78、tests/test_compaction.py（6 用例） | 14 passed |
| E | MCP search 本地语料非真联网 | `SEARCH_API_BASE`(+可选 `SEARCH_API_KEY`) 设置时 httpx GET（10s 超时）+ 通用 JSON 字段归一化（results/organic/web/data/items）；失败/未设置静默回退本地语料，结果带 `source: "web"\|"local"` | servers/search.py:86-158、tests/test_mcp_search.py（6 用例） | 15 passed |

## 2. Wave2-D（graph.py 归拢）

| 项 | 短板 | 改法 | 证据 |
|---|---|---|---|
| D1 | critic LLM 复核流式路径 100% 短路（`_try_llm_critic_sync` 检测到事件循环即 return None，astream 主路径形同虚设） | 改原生 `async def _try_llm_critic`（graph.py:39），critic_node 转 async 直接 await + `asyncio.wait_for` 15s，失败降级规则校验；保留 temperature=0.2 + JSON `{"pass","reason"}`；移除 pytest 双重短路 | graph.py:39-90、tests/test_agent_hardening.py（monkeypatch 断言真实 await） |
| D2 | 多智能体产出任务未真正写库 | executor_node 转 async（graph.py:268），经 `registry.execute_tool("write_tasks")` 全生命周期落库；结果写 `state["task_persist"]`；失败/降级 ERROR 事件+state 透传不崩链路；plans.py:443-447 优先采信落库结果，:478 日志新增 persist 摘要 | graph.py:268+、plans.py:443-478 |
| D3 | critic replan 与周度 reflector 自演进未打通 | `PlanStateEx`（graph.py:17，`task_persist`/`replan_reasons`；节点注解必须用子类否则 LangGraph 1.x 静默丢 key——实证）；critic 跨轮累积 replan_reasons 并随 AgentRunLog 落库（plans.py:474-479）；reflector.py `_replan_stats` 统计 replan 率、`>0.5` 触发 `simplify_decomposition`、LLM prompt 注入重演率；顺带 reflector BLE001/S110 基线 19→0 | graph.py:17+393、reflector.py:22+65+217、plans.py:474-479 |

## 3. 执行记录（2026-09-05 主控复跑）

| 验证 | 结果 |
|---|---|
| pytest 全量 | **89 passed** (216s)（54→89，新增 35 用例） |
| npm run check（turbo: vue-tsc+vite build） | 1 successful (7.14s) |
| lint:eslint | 0 errors / 5 warnings（不变） |
| ruff BLE001/S110/RUF100（agents/scheduler/graph/mcp） | All checks passed |
| 双份 app/ 71 .py | 字节级一致（修复 Wave1 三文件行尾 CRLF/LF 漂移） |

## 4. 遗留与设计边界

- compaction 仍为字符预算近似 token（tiktoken 不引入，符合栈锁定）
- reflector `evaluate_patch_effectiveness` 3 周 delta 为估算值（数据积累后换 per-task 标记，承接 L1-L12 遗留②）
- prod SSE EventSource 鉴权头风险未在本轮范围（见 待优化与补全清单 v0.5 遗留①）

## 5. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-05 | v1.0 | 首版：勘察 5 短板 → Wave1(A/B/C/E)+Wave2(D) 两波子 agent 落地；pytest 89 passed；双份 app/ 0 diff |
