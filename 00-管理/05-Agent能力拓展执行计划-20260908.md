# Agent能力拓展执行计划 — 20260908

> 版本 v0.9_20260908 | 前置：04基线 + 02-需求07设计 | 方式：5路子智能体并行（A文档/P1工具/P2自主/P3协作/P4记忆，文件域隔离，graph.py归拢兼容）| 人力：单人全栈0.5人日

## 1. 技术栈不变声明

Vue3+FastAPI+LangGraph+pgvector不改；新增依赖仅说明不安装（联网搜索用已有httpx，偏好抽取纯函数无依赖）。

## 2. 分工与证据

| 项 | 文件域 | 证据 | 测试 |
|---|---|---|---|
| P1工具 | `registry.py + graph.py executor + mcp.json + skills/calendar_create,todo_create,web_search` | `calendar_create(title/start必填end>start)/todo_create(title+priority1-5)/web_search(query+top_k1-10)`，executor `require_calendar`默认关 | `tests/test_tools_extended.py 4 passed` + `test_registry_write+hardening 18 passed` |
| P2自主 | `graph.py should_replan<3+轻轮过滤 + planner.py patch_id幂等 + plans.py _try_calendar_sync/done.approved` | `patch_id uuid16 + auto_execute + retry_policy`，第三轮无重键 | `test_agents_graph+reflector+compaction 16 passed`，审批2流通过 |
| P3协作 | `state.py _review + graph.py reviewer_node + prompts/reviewer.md + system_agent 0.4.0 + plans AGENT_ORDER` | 7节点6边`critic→reviewer→mentor` | `test_agents_graph 5 passed`，旧6条pending兼容 |
| P4记忆 | `memory.py extract_preference_hints/asearch_with_hints + reflector normalize_patch泛化 + stats/experiments self-evolution` | `estimated`口径不变 | `test_memory_evolution 3 passed` + `memory/rag/reflector 18 passed` |
| A文档 | 04/07/05 + README索引 | 本计划 | 只读/文档 |

## 3. 已知遗留

- `test_persist_order_critic_blocks_no_write` 因P2放宽`<3`预期`replan` vs 旧`mentor`，与reviewer无关，需随后续更新预期。
- `test_plans_sessions_aggregate` 断言6节点，现7节点需更新为7。
- `plans.py:1093`直插回退仍绕过registry严格校验（有意保留防链路崩溃，后续统一）。

## 4. 验证命令

```bash
pnpm run check
PYTHONPATH=apps/server pytest apps/server/tests/test_tools_extended.py apps/server/tests/test_memory_evolution.py -q
PYTHONPATH=apps/server pytest apps/server/tests/test_agents_graph.py -q
npm run build --prefix apps/frontend
curl http://localhost:8000/api/v1/agent/manifest
```

## 变更日志

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-08 | v0.9 | 首版：5路并行执行计划，P1-P4落地记录 |
| 2026-09-08 | v0.9.1 | 审核收口：B1 todo priority透传/B2日历走registry+空跳过/B3 mentor消费reviewer/T1断言<3对齐；详见06-新增内容代码审核 |
