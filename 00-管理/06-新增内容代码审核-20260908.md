# 新增内容代码审核 — 20260908

> 版本 v0.9_20260908 | 范围：P1三工具/P2自主/P3 Reviewer/P4记忆 + 附带小 hardening（auth限流/llm空base guard/工作台竞态）| 方法：diff -w 全读 + 定向pytest | 状态：3修复已落地，1 stale断言已更新

## 1. 结论

- 阻断性bug：0；已修复实质问题3个；剩余风险2个（均为设计权衡，已记录）。
- 定向回归：`test_tools_extended+test_memory_evolution+test_reflector_patch_realloc 12 passed`，`test_persist_order+user_id_passthrough 2 passed`。

## 2. 问题与修复

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| B1 | `app/agents/tools/registry.py:596 todo_create` | 校验`priority 1-5`后调用`_call_mcp_or_mock`时只传`{title}`，priority丢失，下游按默认处理 | 改传`{title, priority}`，与calendar的start/end透传对齐 |
| B2 | `app/api/v1/plans.py:599 _try_calendar_sync` | 直调MCP `calendar/create_event`，绕过registry `calendar_create`校验；`start=""`空串仍调用，靠远端拒错计数失真 | 优先走`registry.execute_tool("calendar_create")`（带schema校验），无registry才回退MCP；空`start`直接`continue`跳过 |
| B3 | `app/agents/graph.py:550 mentor_node` | P3 reviewer仅写`_review+_thought`，mentor/reflector均不消费，reviewer成观测死端，低分不触达用户 | mentor消费`_review`：`score<80`时encourage追加`复核X分，已标N个风险，先修首项`；纯附加不改原分支 |
| T1 | `tests/test_agent_hardening.py:239` | P2放宽`should_replan rewrites<3`后旧断言`rewrites=2→mentor`失效（05已预告） | 更新为`2→replan, 3→mentor`，与critic `replan_reasons<3`对齐 |

## 3. 复查通过项（抽样）

- P1 `calendar_create(title/start必填,end>start)/web_search(top_k/limit 1-10)` 校验完整；executor双写默认关+逐条try/except+`calendar_sync{enabled,tried,ok}`计数正确。
- P2 `should_replan<3` + planner/reflector轻轮过滤（仅`reorder/add_buffer`+元信息）一致；`patch_id uuid16` + `_SEEN_PATCH_IDS 5000上限`幂等有效；`done.approved`齐全；compaction保`approval_required`未动。
- P3 `state._review` + `reviewer_node`纯函数（空任务0分）+ `build_graph critic→reviewer→mentor` + `AGENT_ORDER/LABEL` + 7条log + inspector兼容旧6条pending；前端仅防御性`?? []`+reviewer kind。
- P4 `extract_preference_hints`纯函数（显式键优先/星期众数/薄弱正则/±0.5均值钳制）+ `asearch(with_hints=False)`默认list不变；`normalize_patch`泛化`reduce_*`；`self_evolution after=None`回退before不伪增益，`estimated`口径不变。
- 附带：`auth.py register/login哨兵0限流`、`llm.py空base guard`、`workbench.ts ticket take/approval去重/graph竞态守卫`均安全。

## 4. 遗留风险

- R1 `_SEEN_PATCH_IDS`进程级全局：跨trace的uuid碰撞概率可忽略，但单测复用固定patch_id会误短路；建议后续加`clear_seen_patch_ids()`供测试或改per-trace键。本轮仅文档化，未改结构。
- R2 `test_plans_sessions_aggregate`仍断言6节点，现7节点待更新（05遗留②未动，需另起单测任务）。

## 变更日志

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-08 | v0.9 | 首版：B1-B3修复 + T1断言更新 + 定向回归记录 |
