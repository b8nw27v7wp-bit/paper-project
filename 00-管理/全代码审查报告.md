# 全代码审查报告 — 逻辑检测 + 代码质量

> 版本 v0.1_20260823 | 范围 `E:\paper project` 全量 (P0-P3) | 方法：ruff/vue-tsc/pytest/live + 人工走查 | 执行36次

## 1. 总览

- **Monorepo**：`pnpm-workspace.yaml` 6 workspaces，`turbo` 管线正常，`docker-compose.yml` 4服务与文档一致
- **后端**：FastAPI + SQLModel(Alembic) + LangGraph + pgvector回退 + Neo4j回退 + Redis占位 + APScheduler + MinIO占位；35 tests 100% pass，`ruff` 17剩余（非阻断）
- **前端**：Vue3+Vite+Naive+T ailwind+FullCalendar+ECharts，`vue-tsc` 0 error，`vite build` 5.4s
- **桌面**：Electron40 + better-sqlite3(回退) + sidecar + Tray，`tsc -p tsconfig.electron.json` 0 error

## 2. 逻辑检测

| 模块 | 逻辑点 | 判定 | 说明 |
|------|--------|------|------|
| **Goal** | deadline>now+1d | ✅ | `goals.py:15` 含tz处理，边界测通过 |
| **Task** | end>start, priority1-5, status枚举 | ✅ | `tasks.py:23` 66 80，边界全过 |
| **Complete** | duration0-600 rate0-1 + 二次提交过期修复 | ✅ | 已修 `tasks.py:115` `log_data` |
| **Plan单** | mock 14天 horizon / per=hours/count | ✅ | `planner.py:16` 已加长 |
| **多Agent** | Planner→Critic(重叠/>4h)→Mentor 2轮 | ✅ | `graph.py:52` 5单测 pass，拦截率>20% |
| **记忆** | hash mock + 子串0.85补偿 + Top5 | ✅ | `memory.py:84` 3用例 pass |
| **RAG** | 512/50切块 + 向量 + 三元组 | ✅ | `rag/chunk.py:1` `graph/extract.py:1` 2用例 pass |
| **MCP** | mock 95%+ 超时5s | ✅ | `mcp/client.py:1` 4用例 pass |
| **反思** | 7天完成率/拖延率 + patch | ✅ | `scheduler/reflector.py:1` 4用例 pass |
| **Stats** | overview/trend/experiment | ✅ | `stats.py:1` 手动 curl 200 |
| **事务** | Goal删级联Task, Task删级联Log | ✅ | `goals.py:108` `tasks.py:156` |
| **SSE** | plan_store内存+DB回退 | ✅ | `plans.py:138` 重启后200 |
| **分页** | page/size 1-100 + total | ✅ | 8边界用例 |

## 3. 代码质量 (ruff/vue-tsc)

- **ruff** `app` 17剩余：`BLE001` 8处裸`except`（保留兜底，已在`--fix`后）、`B008` 已`pyproject.toml:50` ignore、`C401/F841`等非阻断；关键`E/F` 0
- **vue-tsc** 0 error；**tsc.electron** 0 error
- **风格**：`UP017 datetime.UTC` 已 `ruff --fix` 批量；`I001` 已排序；`__all__` 已排

## 4. 安全

| 项 | 现状 | 风险 | 建议 |
|----|------|------|------|
| 硬编码 | `config.py:1` 默认password/minio/jwt为dev占位，明文种子 `database.py:52` demo/demo | 低 (dev) | 生产走 `.env` + Keyring，已在 `README.code.md:59` 说明 |
| 注入 | 全ORM参数化，`test_sql_injection` pass | 无 | 保持 |
| XSS | 后端原样存，前端Naive UI自动转义，`test_xss` pass | 低 | 前端已转义 |
| 越权 | `deps.py:1` 固定user1 + `_ensure_goal_owned`，`test_auth_isolation` pass | 中 | P1换JWT+Redis 7d |
| CORS | `*` `main.py:34` P0宽松 | 中 | P1收紧 `cors_origins` |
| 限流 | 未实现，占位 | 中 | P1 Redis 10/min |
| 文件 | 20M/10M/5M 校验 `rag.py:16` `multimodal.py:5` | 无 | 已限 |

## 5. 性能/可观测

- **HNSW** PG未起时走内存余弦，<1k向量<1s；`vector_cosine_ops m=16 ef=64` 已在 `04-DB:124` 定义
- **N+1**：`get_goal` 一次查Task `goals.py:68`，`list_tasks` 先取goal_ids再in，未见N+1
- **懒加载**：前端 `Dashboard` `v-chart` 按需，`FullCalendar` 已懒
- **日志**：`agent_run_log` 全量 + `plan_store` + `langsmith` 占位

## 6. 缺口 (按排期正常)

- Auth JWT 2接口、Pgvector真实HNSW、Neo4j真实连、Redis/MinIO真实、性能k6 20并发 — 均P3前按计划待补
- `better-sqlite3` 编译失败回退 `electron-store` 已验证（`main.ts:22` `initStore`）

## 7. 结论

**高危0 中危3(均P1计划内) 低危4(风格)**，逻辑闭环完整，35 tests + 双构建 0 error，可进入 P3论文阶段。

## 8. 变更

| 日期 | 动作 |
|------|------|
| 2026-08-23 | 全量审查，`ruff --fix` + `plan_store` + `complete` 修 |

