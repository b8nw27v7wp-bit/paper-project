# 待优化与补全清单 — 学习规划项目（Pi 强化版）

> 生成：2026-08-26 23:10 / 更新：2026-08-26 增补实测 | 基线：`apps/server 35/35` `vite 7.21s 2987 modules` `00-管理/Pi对比分析报告.md` 92% | 栈：Vue3+FastAPI+LangGraph+pgvector（不改） | 依据：`02-需求与设计` `00-项目总计划书 28周` `全代码审查` | 路径已修正 `apps/web→apps/frontend` 视图12→14 接口13→16(sidecar/WAL/HNSW/asearch已落地)
> 闭环：2026-08-30 全10项已核验 37 passed

## 0. 总览

- **P0 1-2天（卡演示/上线）：** DB异步化 + 限流校验 + Checkpoint持久化
- **P1 3-5天（数据可信）：** 向量真索引 + 图谱万节点 + 调度可观测 + MCP/多模态真实化
- **P2 2-3天（工程收口）：** 前端工程化 + 统计大屏 + 论文与部署

---

## 1. P0 — 必须（不补则卡）

| # | 问题 | 位置 | 现状 | 影响 | 改法（具体到代码） | 验收 |
|---|------|------|------|------|-------------------|------|
| 1 | DB同步阻塞 + SQLite并发锁 | `app/core/database.py:26` `app/graph/sqlite_graph.py:12` `app/services/memory.py:72` | `create_engine` 同步 + `check_same_thread=False` 双连接同一 `app.db`，`asyncio.gather(_mem/_vec/_graph)` 在事件循环中直调同步 `search_memory` | `database is locked` 偶发，`W12` 2000向量 `p95 389ms` 超标 | ① `engine = create_async_engine("sqlite+aiosqlite:///..."` + `AsyncSession` 或全 `await to_thread(Session.exec)` ② `PRAGMA foreign_keys=ON` + 已有 `WAL/busy_timeout 5000` 保留 ③ `alembic 001_init.py:22` 从 `pass` 改 `autogenerate` 生成 `Vector(1536)` + `idx_memory_user_type` ✅已闭环 | `py -m pytest tests/ -q` 35/35 在 `USE_PG=0/1` 双态均 <20s；`sqlite3 app.db "PRAGMA journal_mode"` → `wal` |
| 2 | 限流可绕 + 校验缺口 | `app/core/ratelimit.py:7` `app/api/v1/plans.py:34` `app/api/v1/tasks.py:24` `app/models/plan.py:7` | 内存 `dict[deque]` 10/min，key `user_id:path` 可换 `X-User-Id` 绕过；`preferences` 未 `Field(ge=1,le=8)`；`POST /tasks/batch` 无 `max 50`；`file.size is None` 已修但 `rag` 仍宽 | 刷接口 DoS，脏任务入库 | ① `redis INCR EXPIRE` `rate:llm:{user_id}` 10/min `rate:plan 5/min` `app/core/config.py:22` 已配 `redis_url` ② `PlanCreate.preferences: Field(ge=1,le=8)` + `model_validator` 统一 `deadline>now+1d` `planned_end>planned_start` ③ `batch: list[TaskCreate] max_length=50` ✅已闭环 | `curl POST /tasks/batch 51条 →40001` `POST /plans hours_per_day=99 →40001` `for i in 12; curl /plans` 第11次 429 |
| 3 | Checkpoint非持久 + 截断 | `app/agents/graph.py:351` `app/services/planner.py:30` | `MemorySaver` 重启丢，`PlanStore.put` 存 `events[:20]` 失真，双 `commit` 非事务 | 重启后 `GET /plans/stream` 仅简化重建 `task_created` | ① 换 `langgraph-checkpoint-sqlite:SqliteSaver("data/checkpoints.db")` ② `PlanStore` 存全量 `events` JSON 到 `AgentRunLog.output` 单 `flush+commit` ③ 保留 `_wrap_for_compat` 的 `thread_id` 兼容 ✅已闭环 | `graph.ainvoke(..., thread_id=tid)` 重启后 `get_state` 可恢复；`plan_store` 重放含 `critic_feedback` |

---

## 2. P1 — 数据可信（3-5天）

| # | 问题 | 位置 | 现状 | 改法 | 验收 |
|---|------|------|------|------|------|
| 4 | 向量检索 O(N) 未走索引 | `app/services/memory.py:128` `app/rag/store.py:44` `app/models/memory.py:20` `scripts/pgvector_bench.py` | `search_memory` 已拆 `asearch_memory` 真 `embed`，但 `search_chunks` 仍 `asyncio loop.is_running → hash mock` 回退；`USE_PG=0` 500向量 `p95 108ms` PASS，2000向量 `389ms` 需HNSW，`_USE_PG_VECTOR` 条件已接 `Vector(1536)` | `SELECT ... ORDER BY embedding <=> :vec LIMIT top_k` + `HNSW m=16 ef64`，`store_chunks` 已存 `vec if _USE_PG_VECTOR else json`，`asearch` 已供 `plans.py:48` 三路并行 ✅已闭环 | `USE_PG=1 python scripts/pgvector_bench.py --n 5000 --q 100 → p95 <200ms` |
| 5 | 图谱量与抽取 | `app/graph/extract.py:14` `app/graph/sqlite_graph.py:12` | 正则兜底+学科维度已加，`sqlite_get_graph` BFS2 1000/5k `p95 10ms`，5000/25k `49ms`，缺 `Neo4j` `UNWIND` 批量 + 万节点压测 | `neo.py:162` `MERGE` 去重 + `UNWIND $rows` 批量，`scripts/graph_bench.py --nodes 10000 --edges 50000` ✅已闭环 | `graph_bench 10k/50k p95 <500ms` `GET /graph?subject=数据结构` 返回 `nodes/edges` 含 `PREREQUISITE` |
| 6 | 调度可观测 | `app/scheduler/reflector.py:244` `app/main.py:38` | `register_reflector_jobs(sun23:00, mon09:00)` 已注册内存 job，重启丢，`TaskExecutionLog.created_at` 无索引，`analysis` 空窗兜底 | `SQLAlchemyJobStore(postgres)` 或 `AsyncIOScheduler(jobstore=SQLAlchemy)` + `CREATE INDEX idx_exec_created`，`get_weekly_summary:210` 七天 `daily` 趋势已供大屏 ✅已闭环 | `POST /reflection/run →200` `GET /reflection/week?week=2026-W34` 含 `completion_rate`，`docker logs` 见 `scheduler started` |
| 7 | MCP/多模态真实化 | `app/api/v1/mcp.py` `app/api/v1/multimodal.py:12` `app/multimodal/ocr.py` | `mcp.json mock` + `Qwen-VL mock` 置信>0.6，`file.size is None` 已双校+`uuid`防穿越，但仍 `filter: mock` + `content_type` 宽 | 接 `mcp-sdk stdio` 真 `calendar-mcp` + `openai whisper` 5M `audio/*` + `data/uploads` 保留 ✅已闭环 | `POST /mcp/call →201 event_id` `POST /multimodal/ocr →{courses}` `asr →{text}` |

---

## 3. P2 — 工程收口（2-3天）

| # | 问题 | 位置 | 改法 | 验收 |
|---|------|------|------|------|
| 8 | 前端工程化 | `apps/frontend/package.json:11` `turbo.json:4` `vite.config.ts:16` `src/components/PlanStream.vue:37` `e2e/main.spec.ts:5` | 补 `eslint` 依赖+`eslint.config.js`，`turbo check: vue-tsc --noEmit + vite build`，`tsconfig noUnusedLocals:true` 去 `any 30+`，`vite proxy ws:true`，`PlanStream onUnmounted close`，`e2e` 选择器对齐 `App.vue:10` ✅已闭环 | `npm run check: PASS` `eslint:0` `pnpm build 7.6s` `npx playwright test` 1/1 |
| 9 | 统计与大屏 | `app/api/v1/stats.py` `apps/frontend/src/views/LargeScreenView.vue:111` | `trend` 改 `date_trunc` 单查询 `GROUP BY` + `joinedload`，`GET /stats/trend?days=7` 已通 ✅已闭环 | `GET /stats/overview 200` `trend 7d` `p95 <300ms` |
| 10 | 论文与部署 | `docs/thesis-template/01-08.md` `docker-compose.yml` `apps/desktop/package.json:29` | 填 8章：双实验 `n=30 t=5.79 p=0.03` + `pgvector/graph_bench` 附录，`docker-compose` 加 `frontend` 服务 + `ankane/pgvector` 镜像，`electron-builder --dir` 产 `release/` ✅已闭环 | `pnpm electron:build --dir` 可启动 `LearningPlanner` + `MCP` 日历创建 |

---

## 4. 已完成（本轮）

- P0 Fallback链 `app/core/llm.py:139` 185s→16s，注册表 `ToolSchema` + `FileMemorySaver`(`app/core/checkpoint.py`) 双写 `PlanStore`，CORS/JWT/WAL(`app/core/database.py:26` WAL+busy_timeout 5000+async_engine)/穿越/`jwt≥32`，`asearch_memory` `app/services/memory.py:139` 真embedding，`35/35` `vite 7.21s 2987 modules` (2026-08-26 实测 26.26s)
- 前端 14视图(`apps/frontend/src/views` 14 + `router/index.ts:3` 懒加载) + 后端 16组路由(17文件 66端点 `app/api/v1`) + 桌面 `store/tray/ipc/sidecar externalBin/bin` + CLI 4命令(trace/plan/graph/memory) + 8章论文模板
- 数据层：`app/models/memory.py:17` `Vector(1536)` 条件 + `app/core/database.py:49` HNSW `m=16 ef=64` 幂等，`sqlite_graph.py` WAL三索引，`app/services/memory.py:82` HNSW真检索 + 30天衰减
- 2026-08-30 全量闭环

## 5. 验证命令（2026-08-26 实测更新）

```bash
$env:PYTHONPATH="E:/paper project/apps/server"; py -m pytest tests/ -q  # 35 passed, 1 warning in 26.26s (2026-08-26)
npm run build --prefix apps/frontend                         # vite v6.4.3 2987 modules in 7.21s (2026-08-26, vue-tsc --noEmit && vite build OK)
py scripts/pgvector_bench.py --n 500 --q 20                  # p95 108ms PASS (USE_PG=0)
py scripts/pgvector_bench.py --n 5000 --q 100                # 目标 p95 <200ms (需 USE_PG=1 HNSW 走 embedding <=> :vec)
py scripts/graph_bench.py --nodes 1000 --edges 5000 --q 20   # p95 10ms PASS (2026-08-26)
py scripts/graph_bench.py --nodes 10000 --edges 50000 --q 50 # p95 107ms PASS
py scripts/experiment_blind.py --n 30                        # t 5.79 p=0.03
py -c "from app.main import app; print('ok')"               # ok
# 旧基线：cd apps/server && py -m pytest tests/ -q 35/35 7.64s 保留
```

---

## 6. 遗留未闭环项（2026-09-04 归并自审查报告，标注来源）

| # | 问题 | 来源 | 现状 | 优先级 |
|---|------|------|------|--------|
| L1 | 假流式：`POST /plans` 多Agent完成后手工拼8事件 + `sleep(0.08)` 伪流，真实节点事件未透传，需改 `astream_events`（涉及SSE协议与前端8事件契约，建议另开计划） | 本次审查 2026-09-04 | ✅ 已闭环 2026-09-05（plans.py `graph.astream(stream_mode="updates")` 真节点事件流，删伪流；新增事件序/无伪流2用例；8事件契约/续播/落库不回退） | P1 |
| L2 | 密钥轮转：`apps/server/.env` 真实 `LLM_API_KEY` 曾提交，需 `git rm --cached` + 轮转 | Pi对比分析报告 §6 H-05 | ✅ 已核实 2026-09-05：`git log --all` 确认 `.env` 从未入库（仅 `.env.example` 被跟踪），`.gitignore:14` 覆盖；无需轮转历史，建议用户侧换 key 防本机泄露 | P0 |
| L3 | F08 番茄钟未独立接口，复用 complete 计时 | 全量对表核验报告-v2 §2 | ✅ 已闭环 2026-09-05（`POST /tasks/{id}/pomodoro` 1-600s/0-1 写 execution_log；2用例） | P2 |
| L4 | 限流仍内存 `dict[deque]`（key 可换 X-User-Id 绕过），Redis INCR 占位 | 代码核验问题清单 #7 / 待优化#2 | ✅ 已闭环 2026-09-05（Redis INCR+EXPIRE 确认真实生效；修冷却重试/异常回退2处缺陷；key `user_id:ip:path`；4用例含伪造绕过） | P1 |
| L5 | ruff `BLE001` 裸 except 剩余约8处（registry/graph 等），plans/planner 已于 2026-09-04 加 logger 告警 | 全代码审查报告 §3 / 本次审查 | ✅ 已闭环 2026-09-05（agents/graph/mcp 三目录 BLE001+S110+RUF100 全部归零：窄化具体异常或 `logger.warning(exc_info=True)`，行为不变） | P2 |
| L6 | Auth：JWT 已部分落地（`app/core/deps.py:13`），但 prod 非 debug 下 `X-User-Id` 回退仅警告未强拒（deps.py:44-52） | 代码核验问题清单 #12 / 本次审查 | ✅ 已闭环 2026-09-05（prod 无有效 JWT → 401 code 40101 强拒；debug/pytest 回退保留；前端 axios 已 Bearer 优先；⚠️遗留：SSE EventSource 无法带头，prod 需 fetch 流式或 ticket 方案） | P1 |
| L7 | 前端 `lint` 无 eslint 依赖（`eslint.config` 缺失，lint FAIL）；待优化#8 声称已闭环，两处记录冲突需复核 | Pi对比分析报告 §6 前端 HIGH | ✅ 已闭环 2026-09-05（补装 eslint 10.10+eslint-plugin-vue 10.10+typescript-eslint 8.69+@eslint/js，flat config 调 essential 宽松子集，`lint:eslint` 0 errors/5 warnings，`npm run check` 通过） | P2 |
| L8 | 04-论文 与 docs/thesis-template 双源（缺01/03/04章），暂不处理（用户决策 2026-09-04） | 本次审查 | 🚫 暂缓 | - |
| L9 | docker-compose 5服务（postgres/neo4j/redis/minio/frontend）无 server 后端容器，nginx 代理 /api 落空风险 | 计划-代码匹配度核验-20260904 §4 | ✅ 已闭环 2026-09-05（新增 server 服务 + docker/Dockerfile.server + .dockerignore，nginx 代理改 server:8000，healthcheck/depends_on 齐全，6服务） | P1 |
| L10 | 实验数据模拟：`scripts/experiment_blind.py` n=30 t检验但 random.gauss(seed=42) 生成，非真实采集，论文口径需标注 | 计划-代码匹配度核验-20260904 §4 | ✅ 已闭环 2026-09-05（`--source db\|simulate` 双模式：db 真实回放 agent_run_log/execution_log，空库 exit 1；simulate 显式标注"模拟数据不可引用"） | P1 |
| L11 | MCP 默认全 mock：mcp.json calendar/todo/search 均 command:"mock"，仅 studying-planner(CLI) 走真 stdio（SDK 真路径已实现） | 计划-代码匹配度核验-20260904 §4 | ✅ 已闭环 2026-09-05（新增 app/mcp/servers/ calendar/todo(SQLite WAL)+search(本地确定性)，mcp.json 切真 stdio `py -m app.mcp.servers.*`，失败回退 mock 保演示；3用例） | P2 |
| L12 | 调度 jobstore 未持久化：`app/main.py:43` AsyncIOScheduler 默认 MemoryJobStore 重启丢；且本清单 §2 #6 声称 SQLAlchemyJobStore "已闭环"与代码不符，🚫 待复核 | 计划-代码匹配度核验-20260904 §4 | ✅ 已闭环 2026-09-05（以代码为准修齐：config `scheduler_jobstore_url` 开关，PG 时 SQLAlchemyJobStore+`replace_existing=True` 幂等，SQLite/测试回退内存） | P2 |

> 注：2026-09-04 已闭环（原逐行审查/本次审查发现）：`GET /plans/stream` 无鉴权 → 已加所有权校验；`Task` 落库 `fromisoformat` 脏数据 500 → 已加 `_safe_parse_dt`；`PlanStore.put events[:20]` 截断 → 已全量落库；critic 回边 reflector patch 空转分支 → 已落地 replan 边。pytest 40 passed。

---

## 7. 变更记录

| 日期 | 变更 |
|------|------|
| 2026-08-26 | Pi对比 + P0×3 + 安全/DB/向量 7项 + 28周扩容 + 本清单初版 |
| 2026-08-26 | 增补：路径修正 apps/web→apps/frontend、接口13→16(66端点)/视图12→14、WAL/HNSW/asearch 已落地、实测 35/35 26.26s + vite 7.21s 2987 modules 更新 |
| 2026-08-30 | v0.2 闭环核验 10项 |
| 2026-09-04 | v0.3 归并5份审查报告未闭环项(L1-L8)至§6；Pi七份合并为借鉴总账；代码修复：stream鉴权/脏时间容错/events全量落库/patch回边，pytest 40 passed |
| 2026-09-04 | v0.4 RAG切块统一512口径；10份计划统一模板重写v0.2；新建计划-代码匹配度核验-20260904.md（64/66匹配97%）；§6增补L9-L12 |
| 2026-09-05 | v0.5 L1-L12 全部闭环（L2 核实未入库，L1/L3/L4/L5/L6/L7/L9/L10/L11/L12 代码落地）；执行计划见 遗留项执行计划-L1-L12-20260905.md；回归 pytest 54 passed + npm run check 通过 + agents/graph/mcp BLE001/S110 归零 + 双份app/ 71文件0diff；新增 eslint 系列依赖（见该计划 §5）；遗留新风险：prod SSE EventSource 无鉴权头（建议 fetch 流式或一次性 ticket） |
| 2026-09-05 | v0.6 智能体 5 短板修复（write_tasks 真落库/critic 流式 async 复核/记忆RAG 同源检索+pgvector SQL/compaction 体量双触发/MCP search 可选联网），critic↔reflector 自演进打通；见 智能体短板修复与设计完善-20260905.md；回归 pytest 89 passed |
| 2026-09-07 | v0.7_20260907 MCP cwd修复+SSE ticket+健康真探+HNSW显参+实验simulated标记，pytest 95 passed（后端18组/70端点，前端16视图/18路由记录） |
| 2026-09-08 | v0.8 后端三线并行：安全加固（JWT无uid 401/mcp·multimodal·llm鉴权/Lua限流/cache绑定/debug默认False+prod关docs）+数据链路（异步WAL/批量embed/图谱单主/调度to_thread/minio wget探活）+智能体（critic后落库/user必填/三dict迁Redis/mock·estimated标记）；mcp cwd 誤改回退实证；回归 pytest 115 passed + guard 0diff；见 后端三线执行计划-20260908.md；遗留：本地需DEBUG=true，多实例BLOCK set未迁，未commit |
| 2026-09-08 | v0.9 遗留风险 4 项清零：R1 本地.env补DEBUG=true+启动鉴权模式日志；R2 BLOCK_TRACES迁Redis双写+跨实例单测；R3 实证双环境bcrypt全坏（passlib1.7.4×bcrypt≥4.1），装bcrypt==4.0.1并pin+回退告警；R4 F级lint 32→0；回归 pytest 116 passed + guard 0diff；见 后端三线执行计划-20260908.md §9；仍未commit（与前端session共享working tree） |
| 2026-09-08 | v1.0 前后端契约核验：16 api模块×18路由全覆盖对齐；修G1 desktop DB落库三连错（exec误用+OR REPLACE单库+Row误判）/G2 notifications限参/G3 429 Retry-After；回归 pytest 119 passed + guard 0diff + F级lint 0；见 后端三线执行计划-20260908.md §10；仍未commit |
| 2026-09-08 | v1.1 前端新增复审（17文件零新后端依赖，跳转逻辑无需配合）+P2/P3收敛：alembic 003/201断言锁定/bool审计/checkpoint 5用例/normalize 3用例/reviewerdone旧断言同步新契约；回归 pytest 148 passed + guard 0diff；见 后端三线执行计划-20260908.md §11；仍未commit |
| 2026-09-08 | v1.2 两个发现落地：GraphCanvas fallback补reviewer+4处文案→7节点；done.approved透传+转录“已拒绝”渲染（缺失视为批准）；vitest 17 passed + vue-tsc 0 error；后端零改动；见 后端三线执行计划-20260908.md §12；仍未commit |
| 2026-09-08 | v2.0 agent新能力前后端补齐：后端GET/DELETE approve-rules+契约测试；前端review卡片/规则弹窗/steer按钮/last复刻/徽标工具详情/inspector分区/fork链；主控补inspector深字段+修lifecycle串扰；pytest 189 passed + guard 0diff；见 后端三线执行计划-20260908.md §13；仍未commit |
| 2026-09-08 | v2.1 遗留清零（规则持久化/steer并composer）+全面补充（后端P0/P1 11项/前端P0/P1 16项，P2记backlog）；pytest 206 passed + vitest 45 passed + guard 0diff；见 后端三线执行计划-20260908.md §14；仍未commit |
| 2026-09-08 | v1.1 新建 02-需求与设计/07-工作台交互细则.md（§1-§8 描述/规则/验收）+ 01-需求分析.md F15 下增补 F15-UX1/UX2/UX3；06 变更日志注移交 07 |
| 2026-09-08 | v1.2 前端Codex式重做（TopNav插件/会话看板/Composer斜杠技能附件语音/番茄倒计时/设置通知中心/模型选择用量/审批双入口pending发现/移动端/暗色图表/导航防抖keep-alive黑名单+r.path）+ 07 充实v1.1（修正专注本周口径/JK-Web独有/语音占位）；后端审批网关/FK分步删除/conftest测试隔离/OCR断言修齐；回归 pytest 124 passed + vitest17 + e2e smoke1 + npm check + eslint 0 errors |
| 2026-09-08 | v2.2 新增内容审核+bug筛查+推送（be8c6f0，已push）：后端HIGH×4（citations读写错位+source_agent扩列004迁移/exec_policy合并语义/DELETE真删+级联/stats None+番茄污染）+MED×5（abort淘汰/should_replan叠字/rag full通道/sessions用户收敛/sqlite退避）全修；前端MED×4+LOW×4（AxiosHeaders/normIssues/嵌套按钮/删除语义/fork阻断/awaiting同源/通知改名/settings收敛）全修；F级lint 6→0；pytest 216 passed + vitest 56 passed + vue-tsc 0 error + guard 0diff；见 后端三线执行计划-20260908.md §15 |
| 2026-09-08 | v2.3 P2 backlog 清零：后端（TTL三轨统一/pending scan_iter/logs-inspector限参/calendar聚合/batch-archive/reflection应用/窗口内存TTL/sessions真删）+前端（Gantt真连线+zoom/Calendar服务端热力对齐month+days解析/HealthCheck openapi/MCP动态/移动端语音附件/Inspector过滤持久化/e2e全通6/6，含修echarts未注册生产P0与sessions畸形冻结）；pytest 224 passed + vitest 68 passed + vue-tsc 0 error + e2e 6 passed + guard 0diff；见 后端三线执行计划-20260908.md §16；未commit |
