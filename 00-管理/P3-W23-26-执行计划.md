# P3 W23-26 执行计划 — 评估与打磨

> 版本 v0.1_20260823 | 前置 P2三端 | 依据 00-总计划 P3 + 03-评估与实验设计

## 1. 目标

**交付**：双对比实验可跑通 + ECharts大屏 (完成率/拖延率/负荷/图谱) + 全量逻辑/代码审查报告。

**验收**：
- [ ] `GET /stats/overview?range=7d|30d` `{completion_rate, delay_rate, avg_load}` + `GET /stats/trend` `{dates, rates, loads}` <2s
- [ ] `POST /stats/experiment` 实验A(多Agent vs 单Agent盲评) / B(有记忆 vs 无记忆7日) 框架可跑
- [ ] 前端 `/dashboard` ECharts 折线/饼/图谱，`FullCalendar` 懒加载
- [ ] 全代码 `ruff` 0 E + `vue-tsc` 0 + `pytest` 35+ + 逻辑检测无高危

## 2. 目录

```
app/api/v1/stats.py
apps/frontend/src/views/DashboardView.vue
app/services/stats.py
```

## 3. 验证

1. `curl /stats/overview` 200
2. `/dashboard` 可见3图
3. `pytest` 35+ + `ruff` 0 E

## 4. 后续

- 论文8章写作
