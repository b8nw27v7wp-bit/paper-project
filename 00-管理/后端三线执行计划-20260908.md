# 后端三线执行记录 — 20260908

> 版本 v1.0_20260908 | 项目：基于多智能体协作与记忆增强的自进化学习任务规划与管理的智能体
> 依据：plan 模式审查（API/数据/智能体三路勘察 18 风险）| 基线：HEAD 0e50ae4 + working tree 未提交 647 行（前端 session 并行）· 双份 app/ 71 文件 0 diff · pytest 95 passed
> 执行：Wave-0 主控基线 → Wave-1A/1B/1C 三子 agent 并行（文件域互斥）→ Wave-2 主控归拢
> 栈锁定：Vue3+FastAPI+LangGraph+pgvector，后端零新增依赖

## 0. 全局约束执行情况

- SSE 10 事件契约 + `POST /plans?mode=multi` 6 节点语义未动，响应 JSON 只增不删（ocr 加 mock/confidence，self_evolution 加 estimated）。
- 未碰 `apps/frontend/**`；`plans.py` ticket/approval 语义原样保留；`POST /plans` 同步模型不动。
- 每次改 `apps/server/app` 后字节级同步根 `app/`；终态 guard 0 diff PASS。
- 未 commit（working tree 归属与前端 session 共享，提交由用户统一决策）。

## 1. Wave-0 基线

- 双份 hash 核验：71/71 文件 0 diff（uncommitted 改动两侧已同步一致）。
- 新增 `apps/server/scripts/check_dual_app.py`（CI 门禁脚本：集合+sha256 双向比对，非 0 diff exit 1）。
- 工作快照：`apps/server/app` 11 文件 +647/-37（ticket/approval 等，属前端 session 并行新增，本轮只读复用）。

## 2. Wave-1A 安全加固（P0）

| 改动 | 文件:行号 |
|---|---|
| JWT 无 uid 回退 1 → 401；require_user_id 真校验 | core/deps.py:33-43,56-61 |
| Lua 原子 INCR+EXPIRE；PYTEST 放行改 RATELIMIT_DISABLED=1；阈值可配置 | core/ratelimit.py:50-80 |
| trace 缓存 user 绑定+鉴权比对；TTL 可配；LRU 上限 | core/cache.py（重写） |
| debug 默认 False；新增 rate_limit_default/plan、cache_ttl | core/config.py:15,50-55 |
| prod 关 docs/redoc | main.py:79-80 |
| mcp/multimodal/llm 鉴权补齐；agent manifest 脱敏 | api/v1/mcp.py:28, multimodal.py:11-43, llm.py:8, agent.py:13-48 |
| conftest 猴 DEBUG=true；6 新用例 | tests/conftest.py（新）, test_security.py:247-310 |
| 本地开发说明 | .env.example |

## 3. Wave-1B 数据链路（P0-P1）

| 改动 | 文件:行号 |
|---|---|
| 异步引擎 WAL 监听；exec→execute；种子 IntegrityError 幂等；HNSW 竞态收敛到 init_db | core/database.py:39-50,60-83,149-229 |
| 登录双认（bcrypt 优先/sha256 兼容旧），新 hash 统一 bcrypt | api/v1/auth.py:25-56 |
| search_memory 保留加 Deprecation 指引 asearch；Vector list() 归一 | services/memory.py:166-237 |
| embed 分批 20 并发 + bulk 保存；PG 归一 | rag/store.py:11-56,100-158 |
| 图谱单主 SQLite + Neo4j try/warn；中文 bigram+学科词表兜底 | graph/neo.py:111-158, extract.py:17-78 |
| 调度 run_db/to_thread；独立 weekly_notify_job；删伪增益 | scheduler/reflector.py:309-489 |
| minio 改 wget 探活；server 补 depends_on neo4j/minio | docker-compose.yml:75-76,127-134 |
| 3 新用例 | tests/test_memory_rag_search.py:137-226 |

## 4. Wave-1C 智能体链路（P1）

| 改动 | 文件:行号 |
|---|---|
| 6 节点改序 planner→researcher→critic→executor→mentor；executor 落库 critic 通过后；user_id/session 必填透传 | agents/graph.py:39,161,268-322,379,521-531 |
| researcher 改 execute_tool；get 标 deprecated；write_tasks schema 必填 user_id | tools/registry.py:125,378-400 |
| 伪复核改 llm:degraded 标记 | graph.py:379 |
| _APPROVALS/_STREAM_TICKETS/plan_store 迁 Redis（TTL 300/60/3600）+ 内存回退 + LRU | api/v1/plans.py:33,236,376,968-1328 |
| 无专属 key provider 跳过 | core/llm.py:36,103,150 |
| mock 标记 | multimodal/ocr.py:23 |
| 5 新用例（+approval 等待 25→60s 防抖） | tests/test_plans.py:613-647, test_agent_hardening.py:214-284 |

## 5. Wave-2 归拢（主控）

| 修复 | 说明 |
|---|---|
| test_registry_write 缺 user_id | 补 `"user_id": 1`（tests/test_registry_write.py:117-122），schema 收紧后的用例侧对齐 |
| mcp.json cwd 回退 | Wave-1C 误改 `cwd:"."→"apps/server"` 还原：cwd="." 致子进程 import 根 app/ 副本，SQLite 落库漂移到仓库根 data/（实证：mcp_calendar.db 错位）；已删根 data/ 下 2  stray db；mcp.json 注释补因果 |
| 3 旧限流用例 kill-switch 隔离 | 加 `delenv("RATELIMIT_DISABLED")`（test_security.py:151-179），全量开关运行时不误伤 |
| self_evolution 模拟标注 | services/memory.py:328-381：weeks 条目 + 顶层 `estimated` 标记，结论标注"模拟数据不可引用" |
| F级 lint 收敛 | client.py:579 去 `as e`；test_plans 去未用 import；test_agent_hardening 去未用 import；字节同步根 app/ |

## 6. 回归证据

| 验证 | 结果 |
|---|---|
| `py apps/server/scripts/check_dual_app.py` | 0 diff PASS |
| `py -m pytest tests/ -q`（RATELIMIT_DISABLED=1） | **115 passed**（440s；基线 95 → +20 用例） |
| 不带开关定向：security+registry+p2 | 36 passed（含 429 真触发） |
| `ruff check --select E9,F` 本轮触及文件 | All checks passed |
| 全仓 ruff 默认规则 435 errors | 历史基线（BLE/UP/I 等风格债，非本轮引入，不在本轮范围） |
| `npm run check` | 未跑：frontend working tree 属另一 session WIP，后端契约由 pytest 覆盖 |

## 7. 遗留风险（移交）

1. 本地 dev 需 `DEBUG=true`（config 默认已翻 False），否则 401——联调前通知前端 session。
2. 不带 RATELIMIT_DISABLED 跑 test_plans 会 429（plans 5/min 生产行为正确；全量回归统一带开关，限流由专用单测覆盖）。
3. APPROVAL_BLOCK_TRACES 仍进程内；多实例网关注入需 Redis 化（plans.py 内存 dict 已迁 Redis，BLOCK set 未迁）。
4. 无 bcrypt 后端时新 hash 回退 sha256（环境缺件兜底，行为兼容）。
5. 未 commit：与前端 session 共享 working tree，提交时机由用户定。

## 8. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-08 | v1.0 | 三线并行落地 + Wave-2 归拢；pytest 115 passed；guard 脚本新增；mcp cwd 回退实证修复 |
| 2026-09-08 | v1.1 | 遗留风险 4 项清零（R1-R4），pytest 116 passed，F 级 lint 32→0 |
| 2026-09-08 | v1.2 | 前后端契约核验（16 api 模块 × 18 路由文件全覆盖，参数/包络/字段逐项对齐）+ 3 缺口修复，pytest 119 passed |
| 2026-09-08 | v1.3 | 前端新增复审 + P2/P3  working tree 收敛：alembic 003（user TIMESTAMPTZ）、201 包络断言锁定、bool 收紧审计、checkpoint 5 用例、normalize 3 用例、P3 reviewer/done.approved 旧断言同步新契约；pytest 148 passed |
| 2026-09-08 | v1.4 | 两个发现落地：①reviewer 前端对齐（GraphCanvas fallback 补 reviewer、4 处 6节点文案→7节点；thought/agent-badge/Inspector 本就通用，无事件分发缺口）；②done.approved 透传（plans 类型+store TranscriptItem+转录行“已拒绝”渲染，缺失视为批准兼容旧 trace）；vitest 17 passed + vue-tsc 0 error；后端零改动 |
| 2026-09-08 | v2.0 | agent 新能力前后端补齐（BE/FE 双 agent 并行+主控归拢）：后端 GET/DELETE approve-rules + steer/last/approve-rule 契约测试；前端 review 卡片/规则弹窗/steer 独立按钮/last 复刻/ephemeral 徽/工具详情/inspector 分区/fork 链；主控补 inspector 深字段（replan_reasons/citations/degraded）+修 lifecycle 全量串扰；pytest 189 passed |
| 2026-09-08 | v2.1 | 遗留清零+最全面补充（4 agent 并行+主控归拢）：规则表 global_state 持久化；steer 并入 composer；后端 P0/P1（citations 回填/sessions 内存源+字段/steer TTL+查队/schema_fallback/鉴权限流/内存盒 TTL/async 对齐/sessions 真删/type 白名单/chunk 截断/top_k 透传/calendar 回显）；前端 P0/P1（reviewer 分支/cancelled 优先/awaiting 驱动/批准 resync/401 redirect/真删/多模态入口/snapshot 折叠/成本卡/失败桶/工具详情/MCP schema/429 倒计时/通知降级/settings 收敛/大屏去重）；pytest 206 passed + vitest 45 passed |

## 9. 遗留风险修复（R1-R4，v1.1）

| # | 风险 | 修复 | 证据 |
|---|---|---|---|
| R1 | debug 默认 False 后本地联调 401 | 本地 gitignored `.env` 补 `DEBUG=true`；`main.py:lifespan` 启动打印 `[auth] mode=debug/prod` 明示鉴权模式 | apps/server/.env（未入库）、main.py:35-41 |
| R2 | APPROVAL_BLOCK_TRACES 进程内，多实例网关盲区 | Redis 双写 `plan:block:{trace}`（TTL=approval 超时）+ `_is_approval_blocked` 内存快路径→Redis 回查→fail-open；add/discard 双清；新增跨实例单测（fake Redis 模拟 worker B） | plans.py:217-253（helpers）、:509-529（guard）、:937-946（add/del）、test_plans.py:test_approval_block_cross_instance_via_redis |
| R3 | sha256 回退静默（双环境 bcrypt 全坏） | 实证：passlib 1.7.4 + bcrypt≥4.1 全坏（`__about__` 移除 + 72 字节硬错），双环境一直在 sha256；修：system python 装 `bcrypt==4.0.1`（已验证 `$2b$` 真 hash/verify），requirements.txt + pyproject pin，auth/database 回退路径加 warning/print 告警 | requirements.txt:19、pyproject.toml:27、auth.py:25-36、database.py:175-184,214-222 |
| R4 | 全仓历史风格债 | F 级（E9,F）32→0：25 处 ruff --fix（未使用 import/f-string），4 处手动（state 去 NotRequired、system_agent 去 `as loop`、asr 删无用 weekday 中文表、experiment_blind df→_df）；BLE/UP/I 等其余 400+ 属风格债，非本轮范围 | ruff --select E9,F 全过；回归 116 passed |

> 依赖登记（AGENTS 约束）：`bcrypt==4.0.1` pin（非新增包，requirements.txt:19 + pyproject.toml:27，原因见 R3）。后端仍零新增依赖。

## 10. 前后端契约核验与补充（v1.2）

> 方法：子 agent 盘点 `apps/frontend/src/api/*.ts`(16) + stores(6) 全调用面；主控逐文件核验后端 18 路由的参数/包络/字段。
> 结论：路由 100% 覆盖；auth/goals/tasks/plans/sessions/approve/stats/experiments/reflection/memory/rag/graph/mcp/multimodal/health/llm/agent/desktop 全部对齐（含 DELETE 204、batch 裸数组归一、pending-approvals 无 token、SSE 10 事件）。

| # | 缺口 | 修复 | 证据 |
|---|---|---|---|
| G1 | desktop window-state DB 落库静默死亡三连错：`session.exec(text(), params)` 误用恒抛 + `INSERT OR REPLACE` 仅 SQLite + 取值 `isinstance(row,(list,tuple))` 误判 SQLAlchemy Row（实证行存在但读回 None） | `session.execute` + `_global_state_upsert`（PG ON CONFLICT / SQLite OR REPLACE）+ `_global_state_get` 取首列；失败打 warning 不再吞错 | desktop.py:20-62,87-127,176-187；单测 db_persist（PUT→清内存→GET source=db） |
| G2 | `GET /desktop/notifications` limit 无校验（负数切出怪异切片） | `Query(ge=1,le=50)` | desktop.py:138；单测 limit 0/100 → 422 |
| G3 | 前端无 429 专项处理，后端 429 无退避提示 | 429 统一加 `Retry-After: 60` 头（ratelimit 单点，前端忽略未知头零风险） | ratelimit.py:105,121；单测断言头 |

## 11. 前端新增复审与 P2/P3 收敛（v1.3）

> 背景：前端 17 文件增量经审计为零新后端依赖（加固：redirect 回跳/审批防重/ticket 一次性/resync 单例/校验对齐）；后端 working tree 含 P2/P3 在途演进。
> 审核结论：前端跳转逻辑（TopNav 不变、LoginView 站内 redirect 加固、workbench 404 terminal）无需后端配合。

| # | 内容 | 处理 | 证据 |
|---|---|---|---|
| H1 | alembic 003（user.created_at TIMESTAMPTZ） | 新建 `003_user_tz.py`（仿 002 全 try/except；SQLite 空跑，PG `USING ... AT TIME ZONE 'UTC'`）；链 001→002→003 与 PG SQL 文本已验证（alembic.ini 在 GBK locale 下 CLI 不可用，改直调验证，属历史环境问题另记） | alembic/versions/003_user_tz.py |
| H2 | 包络 201 保留（用户决策） | `test_goals_crud`/`test_tasks_crud` 追加 `code==201` 断言锁定；已验证前端 `isApiEnvelope` 只认结构、17 模块无 code 分支 | test_goals_tasks.py:28,68 |
| H3 | registry bool 收紧审计 | 4 处 `execute_tool` 调用点只传 str/int/list，无 bool 风险；加 `user_id=True → validation failed` 锁定用例 | test_registry_write.py:test_write_tasks_bool_user_id_rejected |
| H4 | checkpoint 零覆盖 | 新 `tests/test_checkpoint.py` 5 用例：roundtrip/put_writes 落键/重启恢复/多 thread 隔离/脏行跳过（实现细节耦合已在文件头注明） | 5 passed |
| H5 | normalize_patch_for_planner 零覆盖 | 并入 `test_reflector_patch_realloc.py` 3 用例（buffer 补 15/reduce 别名映射/hours float 化+未知键保留） | 13 passed（含既有 5+realloc 纳入） |
| H6 | P3 reviewer 致 sessions 超集、P2 done.approved 恒 True 致 2 旧断言失败 | 非回归，系在途设计演进：测试同步新契约（核心 6 节点超集断言；default 流 `approved is True`）；`AGENT_ORDER` 已含 reviewer 一致；前端 graph/nodes 动态渲染不受影响 | test_plans.py:233-238,523-524；全量 148 passed |

## 13. agent 新能力前后端补齐（v2.0）

> 双 agent 并行（BE：approve-rules 端点 / FE：B1-B8）+ 主控归拢。新增端点登记（AGENTS 约束）：`GET /plans/approve-rules`、`DELETE /plans/approve-rules`（内存全局表，与 `_APPROVALS` 同策略；多进程不共享已注明）。

| # | 内容 | 证据 |
|---|---|---|
| BE | approve-rules 增删查 + 403/400/幂等 10 用例；`exec_policy.remove_rule` | plans.py:2579-2620、exec_policy.py:183、test_approval_policy.py:220-280 |
| FE | review 卡片/规则弹窗/steer 独立按钮/last 复刻/ephemeral 徽/工具详情/inspector 分区/fork 链 + waveb 8 用例 | plans.ts:561-660、workbench.ts、AgentWorkbenchView.vue、TranscriptView.vue、vitest 25 passed |
| 主控补 | inspector `state.replan_reasons`（critic 日志现成）/`state.citations`（Task.source_agent 反查）/`state.degraded`（executor persist error）；`test_inspector.py` 4 用例 | plans.py:1538-1600,3074、test_inspector.py |
| 主控修 | lifecycle 事件上限 200 全量串扰：计数断言→事件身份断言（饱和免疫已验证） | test_registry_lifecycle.py:45,62 |
| 回归 | pytest 189 passed + guard 73 文件 0 diff + F-lint 0 + vitest 25 passed + vue-tsc 0 error | — |
| 遗留 | 规则表纯内存（重启丢）；steer 顶栏/composer 双入口未合并；B5 降级 toast 以 state.degraded 为闸（已可触发）；未 commit | — |

## 14. 遗留清零与全面补充（v2.1）

> 4 agent 并行（规则持久化 BE / steer 合并 FE / 后端缺口侦察 / 前端缺口侦察）→ Wave-2 双 agent（BE 后端 P0/P1 / FE 前端 P0/P1）→ 主控归拢。侦察报告原文见任务记录；P2（Gantt 真连线/zoom、日历热力、HealthCheck openapi 化等 15 项）记入 backlog 未做。

| # | 内容 | 证据 |
|---|---|---|
| 遗留1 | 规则表持久化：global_state key='approval_rules' 全量 JSON，启动/访问懒加载，写后同步回 DB，失败 warn 不阻断 | exec_policy.py:23,207,228,264、plans.py:2612,2647,2754、test_approval_policy 12 passed |
| 遗留2 | steer 并入 composer：running 态占位变追问、回车走 steerPlan+toast，非 running 保持新建链；删独立输入框；`utils/composerSend.ts` 纯函数 + 9 用例 | AgentWorkbenchView.vue、waveb→steer-merge.spec.ts、vitest 全过 |
| BE-P0 | citations 回填三回执；sessions 并内存源+ephemeral/degraded/citations 字段；steer TTL+查队+终态拒收；schema_fallback 标记；mcp 列表鉴权；steer/approve/rule/mcp 限流；四内存盒 TTL；sessions DELETE 真删 | plans.py、mcp.py、test_wave2_backend 15 用例 |
| BE-P1/P2 | tasks_async pomodoro+缓存失效；goals_async tasks join；memory type 白名单；rag 截断+has_more；top_k 透传；calendar 回显 | 同上 |
| FE-P0 | reviewer 分支/cancelled 优先/awaiting 驱动/批准后 resync/401 redirect/会话真删（后端已就绪，直连）/多模态入口/snapshot 折叠 | TranscriptView/MonitorPanel/plans/sessions、wave2.spec 11 用例 |
| FE-P1 | 成本卡/失败桶/工具详情/MCP schema/429 倒计时（读 Retry-After）/通知降级/settings 收敛stores/settings.ts/大屏去重 | 同上 |
| 回归 | pytest 206 passed + vitest 45 passed + vue-tsc 0 error + guard 73 文件 0 diff + F-lint 0 | — |
| 遗留 | P2 backlog 15 项；规则多实例后写胜出无锁；store 残留 wb.steer 未删（防引用）；未 commit | — |

## 12. 两个发现落地（v1.4）

> 用户指令：将 §11 的两个发现改掉。结论：后端侧本就齐（AGENT_LABEL/AGENT_ORDER/done.approved 均已落地），缺口全在前端展示层；`onDone` 经查无“approved 不存在”判断 bug，但拒绝态无区分显示，属体验缺口一并补齐。

| # | 缺口 | 修复 | 证据 |
|---|---|---|---|
| F1 | GraphCanvas 空态 fallback 硬编码 6 节点；3 处文案停留 6节点（工作台空态/转录 running/实验 Multi） | fallback 补 reviewer（critic→reviewer→mentor 序）；文案→7节点 | GraphCanvas.vue:45,65-66、AgentWorkbenchView.vue:462、TranscriptView.vue:79、ExperimentsView.vue:39 |
| F2 | reviewer SSE 只有 `thought(agent=reviewer)`，无新事件类型——确认前端无分发缺口（handleThought agent 通用透传 + badge 直显；Inspector agent 下拉为空串=全部） | 无需改，书面确认 | workbench.ts:295-316 |
| F3 | `done.approved=false`（拒绝/超时）时转录仍显示“完成 · 0 任务”，与审批卡“已拒绝”不一致 | `PlanStreamHandlers.onDone` 加 `approved?: boolean`；store `TranscriptItem.approved` 透传（缺失=批准，兼容旧 trace）；转录 done 行 `approved===false` 显示“已拒绝” | plans.ts:69、workbench.ts:48-53,452-462、TranscriptView.vue:63-65 |
| — | 回归 | vitest 17 passed；vue-tsc --noEmit 0 error；后端零改动（pytest/guard 沿用 v1.3 结论） | — |
| 2026-09-08 | v2.2 | ͬ�ϣ������bug+�ύ���ͣ�be8c6f0����push origin/main�� |
