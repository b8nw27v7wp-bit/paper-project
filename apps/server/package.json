{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --port 8000 --host 0.0.0.0",
    "dev:pip": "python -m uvicorn app.main:app --reload --port 8000 --host 0.0.0.0",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "lint": "ruff check app",
    "test": "pytest -v"
  }
}
