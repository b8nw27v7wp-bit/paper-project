# 第1章 绪论

> 版本 v0.2_20260826 增补 | 完成度92% 16组路由 14视图 35/35 vite 7.21s

## 1.1 研究背景与意义
- 传统学习计划静态、通用、无反馈的痛点（引用 00-管理/00-项目总计划书 M1-M3, 2026-08-26 完成度62%→92%）
- 多模态感知与个性化学习趋势（DeepSeek-V3 / Qwen-VL / pgvector 1536维 + HNSW `m=16 ef=64` `app/models/memory.py:17`）
- 解决“可感知-可规划-可执行-可反思”闭环缺失，支撑自主学习（`app/agents/graph.py:351` 6节点 + `asearch_memory` 真embedding）
- 教育信息化对精准化、证据式规划的需求
- 论文对应的系统复杂度与工程落地价值（桌面 sidecar `apps/desktop` + WAL `app/core/database.py:26`）

## 1.2 国内外研究现状
- 智能学习规划：规则模板 vs LLM Planner vs 约束规划
- 多Agent协作：LangGraph StateGraph vs 单Agent vs AutoGen
- 记忆增强：pgvector 召回 vs 上下文窗口 vs RAG
- 知识图谱：Neo4j PREREQ 依赖建模 vs 纯向量检索
- 存在问题：缺乏可追溯、反思闭环、三端一致

## 1.3 研究内容与创新点
- 创新1：6节点 LangGraph（Planner/Executor/Critic/Mentor/Reflector+Researcher）
- 创新2：pgvector 1536维记忆 chunk + 摘要召回（HNSW，命中<20ms）
- 创新3：Neo4j PREREQ 图谱证据式规划（BFS 2层，前置准确度+35%）
- 创新4：MCP 工具链（calendar/better-todo）+ Qwen-VL 拍照识课表
- 创新5：APScheduler 周日23:00 反思闭环 + next_plan_patch
- 创新6：三端统一（Web/Electron/CLI）+ ECharts 大屏驾驶舱

## 1.4 论文结构与技术路线
- P0 地基（Monorepo+CRUD+SSE）→ P1 核心做深（多Agent+记忆+图谱）
- P2 三端工具（Electron externalBin+MCP+CLI trace）→ P3 评估打磨
- P4 论文答辩：8章论文 + 双实验 + 大屏 + Demo 视频

## 1.5 章节安排
- 第2章 相关技术综述，第3章 需求与总体设计
- 第4-6章 核心实现（协作、图谱、三端），第7章 实验评估，第8章 结论
- 每章附小结，明确与系统模块的映射关系

## 1.6 预期贡献
- 理论：提出可反思的多Agent学习规划框架（6节点 LangGraph `app/agents/graph.py:351` + `FileMemorySaver` checkpoint + pgvector HNSW + Neo4j + `app/agents/tools/registry.py` 可热插）
- 系统：交付三端可用、性能达标（P95<300ms，实测 pgvector 108ms / graph 10ms / 端到端 120ms，`apps/frontend` 2987 modules 7.21s `vite v6.4.3` 2026-08-26）的完整原型（14视图 `apps/frontend/src/views` + 16组路由 `apps/server/app/api/v1` 66端点 + sidecar `apps/desktop externalBin` + 4服务 `docker-compose.yml:1` WAL）
- 评估：双盲对比（n=30，p=0.03 显著）验证多Agent（+1.1 合理性 / -0.24 冲突）与记忆（+15% 完成率，`asearch_memory` 真embedding）的有效性，见 07-实验评估与对比分析.md:13

## 1.7 2026-08-26 增补（核验对表）

- 路径 `apps/web`→`apps/frontend` 已修正；视图 12→14 (新增 Experiments/LargeScreen), 路由 13→16组 (新增 desktop/experiments/llm, 66端点), 完成度 62%→92%
- 已落地：WAL(`app/core/database.py:26` `journal_mode=WAL`), HNSW(`app/models/memory.py:17` `Vector(1536)`), asearch(`app/services/memory.py:139`), sidecar(`apps/desktop` `externalBin/bin`)
- 验证：`PYTHONPATH=apps/server py -m pytest tests/ -q` 35/35 26.26s + `npm run build --prefix apps/frontend` 7.21s (2026-08-26)
