# 第2章 文献综述与相关技术

> 版本 v0.2_20260826 增补 | 路径 `apps/web`→`apps/frontend` 视图12→14 完成度92%

## 2.1 智能学习规划研究综述
- 目标-任务分解模型（Gantt/Calendar、work breakdown）
- 学习任务调度：约束规划 vs 启发式 vs 强化学习
- LLM 在教育规划中的适用性与幻觉风险

## 2.2 大语言模型与 Prompt 工程
- DeepSeek-V3 / Qwen 统一 LLM 封装（app/core/llm.py provider无关）
- SYSTEM_PROMPT 约束 JSON 数组输出，含重试与 faux 兜底
- SSE 流式返回与可追溯 trace_id（首字节<2s，心跳3000ms）

## 2.3 多智能体协作框架
- LangGraph StateGraph 6节点设计（Planner/Executor/Critic/Mentor/Reflector/Researcher）
- agent_run_log 落库可追溯（P1 W9-11），Pi checkpoint 可恢复
- Critic 双校验：规则冲突 + LLM 语义校验，Mentor 个性化激励

## 2.4 向量检索与记忆增强
- pgvector 1536维 + HNSW 索引（app/models/memory.py，USE_PG=1时启用），检索 P95 108ms（top_k=5，n=30 实测）
- 记忆类型：memory / knowledge，支持 subject 过滤
- 流程：切片 → Embedding → 存储 → 检索 → 摘要注入 Planner
- 实验：有/无记忆 7日完成率对照（n=30，p=0.03 显著），盲评 delta +15%

## 2.5 知识图谱与 GraphRAG
- PDF 切片 Embedding + Neo4j 三元组抽取（app/rag/* + app/graph/neo.py），图谱检索 P95 49ms（BFS 2层，n=30）
- PREREQ 前置依赖查询与证据式规划（evidence_chain+coverage），accuracy 0.88 vs 0.60
- 可视：ECharts Graph force，热力按学科密度；实验见 07-实验评估与对比分析.md:27

## 2.6 MCP 与多模态感知
- MCP Python SDK：calendar-mcp / better-todo-mcp（app/mcp/*）
- Qwen-VL 拍照课表 OCR（/api/v1/multimodal/ocr）+ ASR 语音转任务
- 统一工具注册表 app/agents/tools/registry.py（single responsibility）

## 2.7 前端与桌面技术
- Vue3 + Naive UI + FullCalendar + ECharts + Pinia + Vue Router（apps/frontend/src/views 14视图，2987 modules）
- Electron externalBin sidecar、better-sqlite3 windowState、Tray/Notification
- 性能：懒加载路由分包、LRU 缓存、timed P95（pgvector 108ms / graph 49ms / 端到端 120ms，见 app/services/performance.py:60）

## 2.8 技术选型总结
- 后端 FastAPI 16组路由(`apps/server/app/api/v1` 66端点 `app/main.py:27`) + SQLModel(Alembic) + pgvector(HNSW `m=16 ef=64` `app/models/memory.py:17` + WAL `app/core/database.py:26`) + Neo4j + Redis + APScheduler(双Job)
- 前端 http://localhost:5173 代理 /api→8000 `ws:true`(`apps/frontend/vite.config.ts:16`), 14视图 `apps/frontend/src/views` + `router/index.ts:3` 懒加载, `vite build 2987 modules 7.21s` 2026-08-26 必过
- 部署 Docker Compose一键启动 4服务 + 桌面 `electron-builder externalBin/bin sidecar` NSIS/AppImage + 统一LLM(`app/core/llm.py` fallback) + `asearch_memory`(`app/services/memory.py:139`)

## 2.9 2026-08-26 增补

- 完成度 62%→92%, 接口 13→16组, 视图 12→14, 侧车/WAL/HNSW/asearch 均已落地实测 `35/35 26.26s` `7.21s`
