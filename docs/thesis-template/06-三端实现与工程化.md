# 第6章 三端实现与工程化

> 版本 v0.2_20260826 增补 | 完成度92% 14视图 16组路由 35/35 7.21s

## 6.1 前端工程（Vue3 生态）
- Monorepo pnpm+turbo（pnpm-workspace.yaml:1 + turbo.json:4），Vite 代理 /api→8000 `ws:true` + 代理 /health（apps/frontend/vite.config.ts:16）
- UI：Naive UI + Tailwind，路由分包 14视图（Home/Goals/Calendar/Week/Gantt/TaskBatch/KnowledgeGraph/Dashboard/Experiments/LargeScreen/RAG/Reflection/MCP/Health，apps/frontend/src/router/index.ts:3），Apple 克制（#1d1d1f / #86868b / #f5f5f7）（修正 12→14 2026-08-26）
- 性能：路由懒加载、Vite chunk、cached 装饰器（TTL 30s）、LRUCache 512；vite build 2987 modules，7.21s（2026-08-26 实测 `vite v6.4.3 in 7.21s`, 原6.66s→7.21s）
- 指标：timed 埋点 P95/均值（app/services/performance.py:60），X-Response-Time 头；pgvector P95 108ms / graph 10ms / 端到端 120ms（`py scripts/pgvector_bench --n 500 --q 20` + `graph_bench --nodes 10000 --edges 50000 p95 107ms`）
- 路径修正：`apps/web`→`apps/frontend`（2026-08-26）

## 6.2 Electron 桌面端（P2 W17-18）
- electron-builder 配置：appId com.learning.planner，externalBin sidecar 指向 FastAPI
- extraResources：../server（过滤 __pycache__/*.pyc/data/*.db）+ externalBin/bin
- 产物：NSIS（oneClick false）+ dir + AppImage，asarUnpack better-sqlite3/electron-store
- Sidecar：isDev 本地 python -m uvicorn，prod 优先 bin/server 二进制，否则 extraResources/server
- WindowState：better-sqlite3 planner.db（window_state/global_state）fallback electron-store
- Tray：nativeImage 空占位、ToolTip 未读、double-click 唤起、Menu（显示/隐藏/通知/退出）
- Notification：Notification.isSupported() 校验，click 唤起窗口，notify/push 双通道
- IPC：registerMainIpc（app:health/window:*/store:*/notify:*/desktop:sync/shell:openExternal）+ preload bridge

## 6.3 CLI Trace 工具（P2 W19-20）
- Typer 4命令：trace/plan/graph/memory，均 --json 便于 jq 管道
- trace：优先 trace_id → /plans/{trace}/logs + SSE 预览，否则按 goal 展示最近任务
- plan：POST /plans?mode=multi → 自动拉 logs（agent 轨迹表）+ SSE curl 提示，支持 --hours
- graph：GET /graph?subject&keyword → 表格+热力
- memory：GET /memory/search + --ablation 拉 /experiments/memory-ablation 对照
- health：双探针 /health + /api/v1/health，适配 sidecar

## 6.4 后端工程化
- FastAPI 单后端 16组路由 66端点（`apps/server/app/main.py:27` 16 `include_router`, 修正 13→16 新增 desktop/experiments/llm；`apps/server/app/api/v1/*.py` 17文件）：CORS(`_cors_config` dev白名单 + prod过滤 `app://*`)、Validation 40001、global 50001 trace、lifespan init_db
- 统一LLM：`app/core/llm.py` provider无关 + `FALLBACK_ORDER` 4链 + `_is_retryable` 指数退避；工具注册表 `app/agents/tools/registry.py` `ToolSchema/RegisteredTool` 可热插 `memory/rag/graph/write`
- 协作：`app/agents/graph.py:351` 6节点 StateGraph + `app/core/checkpoint.py:FileMemorySaver` + `_wrap_graph_for_compat` thread_id 可恢复；`PlanStore` 内存+DB双写
- 检索：`app/services/memory.py:139` `asearch_memory` 真embedding + `app/models/memory.py:17` `Vector(1536)` + `app/core/database.py:49` HNSW `m=16 ef=64`
- 调度：AsyncIOScheduler register_reflector_jobs（周日23:00 + 周一09:00）`app/scheduler/reflector.py:269`
- 代理：GET/PUT /desktop/window-state、POST /desktop/notify/sync/notifications/config
- 缓存：performance.py LRUCache + cached/timed 装饰器，hit_rate 可大屏展示
- DB：WAL + synchronous=NORMAL + busy_timeout=5000（`app/core/database.py:26`）+ `async_engine sqlite+aiosqlite` + pgvector HNSW 幂等建索引（2026-08-26 已落地）

## 6.5 测试与部署
- pytest 35例全过（`$env:PYTHONPATH="E:/paper project/apps/server"; py -m pytest tests/ -q` → `35 passed, 1 warning in 26.26s` 2026-08-26，`apps/server/tests/test_p2.py:1`），CI faux 兜底；统计 n=30 p=0.03 均在 `experiments.py` 覆盖
- E2E Playwright 前端，`vite build 2987 modules in 7.21s` (2026-08-26, `vite v6.4.3`) 必过，`vue-tsc --noEmit`；`npm run build --prefix apps/frontend` 实测 7.21s（原5-7s→7.21s）
- Docker Compose 一键启动 4服务 postgres(pgvector:pg16)/neo4j:5/redis:7-alpine/minio（docker-compose.yml:1，healthcheck 全量），pnpm-workspace + `turbo check` 已做（`turbo.json:4` + `apps/frontend/package.json:11` `check: vue-tsc --noEmit && vite build`）

## 6.6 2026-08-26 增补（核验）

- 完成度62%→92%，路径 `apps/web`→`apps/frontend`，路由13→16(66端点)，视图12→14，WAL/HNSW/asearch/sidecar 均已落地实测

## 6.6 本章小结
- 三端复用同一 FastAPI/SSE 链路，保证功能一致与可维护性
